# 2026-07-14

## 推送 open-loop 到 GitHub
- 创建仓库: https://github.com/pixcore598-design/open-loop
- 网络: github.com:443 不通，SSH 22 端口通
- 生成新的 ED25519 SSH deploy key (openloop_github_deploy_ed25519)，通过 gh api 添加到仓库
- 原仓库是 shallow clone，对象不完整导致 push 失败，通过 checkout --orphan main 重建干净历史
- 成功推送 7299 文件到 main 分支
- SSH config 更新: github.com → openloop_github_deploy_ed25519

## Loop Engineering 本地验证
- 修复 vitest.config.ts: include 模式增加了 `src/**/__tests__/*.test.{ts,tsx,js,mjs,cjs}`，原配置仅匹配 `tests/`
- 修复 config-loop.ts: `parseStrategy` 的 `toLowerCase()` 会把 `mustFix_only` 转成 `mustfix_only` 导致不匹配 LOOP_STRATEGIES，改为 `find(s => s.toLowerCase() === v.toLowerCase())` 大小写不敏感匹配
- loop-engine.test.ts 19/19 全部通过
- 提交 ed7b831 已推送到 main
