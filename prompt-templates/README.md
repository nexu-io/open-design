# Prompt templates

The legacy image/video prompt catalogue is intentionally empty on this routing
validation branch. The selected rendering Skills are under `design-templates/`;
see `specs/current/skill-discovery-routing-catalog.md` for their pinned source.

Keep this root available for the packaged resource copier. An empty catalogue
is supported by the image/video template APIs.
