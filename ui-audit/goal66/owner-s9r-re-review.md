# S9-R owner re-review — BLOCK

- **PASS** compensation: `apps/daemon/src/routes/collab-sync.ts:1338-1358` catches failed local completion after Vela resume, attempts `stopVelaShare`, returns bounded 502 on compensated stop and a manual-revoke error when stop fails. HTTP S9-R spec toggles `failComplete`/`failStop`, observes stopped/active remote status, then successfully invokes owner DELETE with no local row (`apps/daemon/tests/public-share-origin-http.test.ts:153-235`).
- **PASS** exact file/slug checks and no-upload version preservation: DELETE remote-only requires matching active source file and slug (`collab-sync.ts:1564-1568`); S9-R wrong-file request returns 404. Resume calls only `share resume-existing`, maps its original receipt without rebuilding/uploading bytes (`collab-sync.ts:1334-1343`); spec checks receipt equality, one upload, and command sequence. Wrong slug is source-enforced, but not exercised by S9-R HTTP spec.
- **BLOCK — owner evidence absent on remote-only DELETE**, `apps/daemon/src/routes/collab-sync.ts:1551-1568`: the owner check rejects only a *non-null* catalog owner different from the caller. If `resolveSharedProject` returns null, any verified workspace member with `canShareProjects` and visibility of an active remote project/file/slug proceeds to `share stop` despite having no local publication revision or owner witness. `readProjectShareState` checks active workspace membership, not project ownership (`apps/daemon/src/collab/vela-project-share-state.ts:25-28,42-46`). Fail closed on null catalog for remote-only recovery unless a separate authoritative owner witness proves the caller owns the project; add HTTP wrong-owner and wrong-slug cases with no local revision and assert no stop command.

Validation: `cd apps/daemon && pnpm exec vitest run -c vitest.config.ts tests/public-share-origin-http.test.ts tests/collab-sync-routes.test.ts` — 2 files / 142 tests passed. No browser, candidate source, or tests edited. Residual risk: CLI/Vela backend may independently deny a non-owner, but the daemon route does not establish the claimed owner-only authorization; S9-R spec does not exercise the null-catalog wrong-owner case.
