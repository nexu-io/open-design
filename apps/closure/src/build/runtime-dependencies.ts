/** Closure-owned runtime dependencies. Node/SQLite/PTY are physical Shell inputs. */
export const closureRuntimeDependencies = Object.freeze({
  "@ffmpeg-installer/ffmpeg": "1.1.0",
  "blake3-wasm": "2.1.5",
  "hyperframes": "0.8.1",
});
