// @ts-nocheck
/** @module cli/automation
 * Barrel for the automation domain: re-exports `runAutomation` to the root CLI dispatcher.
 * Automation commands mirror the Automations tab against `/api/routines`; same surface, same DTO shapes.
 */
export * from './automation.js';
