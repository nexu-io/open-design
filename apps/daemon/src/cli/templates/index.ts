// @ts-nocheck
/** @module cli/templates
 * Barrel for the templates domain: re-exports `runTemplates` to the root CLI dispatcher.
 * Template commands are the headless face of the web New Project / Examples panel, managing saved project templates via `/api/templates`.
 */
export * from './templates.js';
