// @ts-nocheck
/** @module cli/export
 * Barrel for the export domain: re-exports `runExport` to the root CLI dispatcher.
 * Export commands rasterize HTML/deck artifacts to PDF, PPTX, or image via the desktop Chromium renderer.
 */
export * from './export.js';
