// @ts-nocheck
/** @module cli/media
 * Barrel for the media domain: re-exports `runMedia` to the root CLI dispatcher.
 * Media commands generate image, video, and audio assets via the daemon's task queue.
 */
export * from './media.js';
