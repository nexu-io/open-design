// Compatibility re-export: the slim charter is shared by daemon and BYOK
// composers from @open-design/contracts so both execution paths stay exact.
export {
  HOST_CLARIFICATION_GATE,
  PLATFORM_CONTRACTS_BLOCK,
  PROMPT_INJECTION_RESISTANCE,
  SLIM_CORE_CHARTER,
  SLIM_PLAN_FOUNDATION,
  renderPlatformContractsBlock,
  renderSlimCoreCharter,
  renderSlimPlanFoundation,
} from '@open-design/contracts';
