/** @module connection/index
 * Database-lifecycle barrel — re-exports `openDatabase` and `closeDatabase`; the singleton state and migration call remain internal to the connection concern. */
export * from './connection.js';
