/** @module locations/index
 * Project-location layer: built-in and user-configured project storage locations, the `.open-design/project.json` manifest, and location scanning/creation primitives.
 * Depends only on core/ (id validation); no sibling subdirectory imports.
 */
export * from './locations.js';
