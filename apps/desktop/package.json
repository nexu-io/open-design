{
  "name": "@open-design/desktop",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "main": "./dist/main/updater.js",
  "files": [
    "dist"
  ],
  "exports": {
    "./updater": {
      "types": "./dist/main/updater.d.ts",
      "default": "./dist/main/updater.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "tauri:build": "tauri build --config src-tauri/tauri.conf.json",
    "tauri:check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "test": "vitest run -c vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json --noEmit"
  },
  "dependencies": {
    "@open-design/diagnostics": "workspace:*",
    "@open-design/host": "workspace:*",
    "@open-design/platform": "workspace:*",
    "@open-design/sidecar": "workspace:*",
    "@open-design/sidecar-proto": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.2",
    "@types/node": "24.12.2",
    "typescript": "6.0.3",
    "vitest": "4.1.6"
  },
  "engines": {
    "node": "~24"
  }
}
