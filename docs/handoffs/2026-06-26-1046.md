---
status: in_progress
position: 시각 리브랜딩 3종 완료·PR#2 main 머지됨(6d42c055e). 다음 = Braze "프로토타입" 배지 수정(C안 설계).
last_updated: 2026-06-26
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약. 재개 에이전트는 각 항목 이해 표명 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 모든 pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`. 시스템 node25(ABI141)≠node@24(ABI137) → better-sqlite3 네이티브 실패.
- [ ] CONSTRAINT: gh 계정 — origin=`Gmin82/open-design`. push 전 `gh auth switch --user Gmin82`(evan2942=403). 현재 active=Gmin82.
- [ ] CONSTRAINT: shell=zsh — `$PIPESTATUS` 빈값(zsh는 `$pipestatus`). 파이프 exit는 `cmd >log 2>&1; echo $?`.
- [ ] CONSTRAINT: i18n 키 추가 시 **19 로케일 전부**(`apps/web/src/i18n/locales/*.ts` it.ts 포함) + `types.ts` Dict. 누락=typecheck 실패.
- [ ] CONSTRAINT: 빌드가 `apps/web/next-env.d.ts` 더럽힘 → 커밋 전 `git checkout apps/web/next-env.d.ts`로 되돌릴 것(리브랜딩 무관 산물).

## 현재 상태
P0 리브랜딩 후 **시각 리브랜딩** 진행. 브랜치 `fu/visual-rebrand`에 3커밋 (origin 미푸시). main 기준 분기.
- 05aaa7d4e — accent #16c5ff + Pretendard 폰트
- 28ca26273 — 홈 표면 정리(서브타이틀·Star/Discord·커뮤니티 갤러리 제거)
- 04162b22b — 닥터 마스코트 아이콘/로고(web + desktop icns/ico/png)

## 완료 (이번 세션, 전부 검증 GREEN)
- **포인트컬러 `#c96442`→`#16c5ff`**: appearance.ts `DEFAULT_ACCENT_COLOR`·layout.tsx pre-hydration fallback·tokens.css 정적(light/dark/media) + 파생 color-mix 동기화. 장식 하드코딩(composio.css·NewProjectPanel SVG) 동반. 펫 마스코트 색은 의도적 보존.
- **폰트 Pretendard**: `--sans`·`--serif` → Pretendard Variable. self-host woff2(`apps/web/public/fonts/PretendardVariable.woff2` 2MB, 오프라인). 신규 `styles/pretendard.css` @font-face + index.css import. `--mono`는 monospace 유지(사용자: 코드는 냅둬).
- **홈 UI 3제거**: ①서브타이틀(HomeHero) ②우상단 Star+Discord(EntryShell, 미사용 import/훅 정리) ③커뮤니티 갤러리(HomeView PluginsHomeSection+HomeTemplatesReveal + 데드 import). 우상단 메가폰/렌치/기어는 유지.
- **아이콘/로고 닥터(C안)**: 소스=bodoc `apps/web/public/app-icon.png`(3D 닥터, 시안). app-icon.png 교체 + HomeHero·EntryNavRail img src .svg→.png. 데스크톱 빌드아이콘 4종(tools/pack/resources mac icns/png·win ico·linux png) PIL+iconutil로 닥터 500→1024 LANCZOS 재생성. (bodoc 데스크톱 아이콘은 OD 그대로라 복사 무의미 → 직접 생성.)
- 게이트: web typecheck PASS · web build PASS · guard 63/63 · tools-pack typecheck PASS · 라이브 스크린샷 3회(시안·Pretendard·제거·닥터 전부 확인).

## 결정 (이번 세션)
- 내부 전용 툴 + 도메인 `open-design.ai` 데드(인프라 미사용) 확정 → SEO 보존(alias/slug/blog본문) 명분 소멸. blog본문 1987·SEO alias·데드도메인 스왑 = **무가치, defer/skip**.
- 폰트 "전부 Pretendard" 하되 코드(--mono)는 monospace 유지(사용자 정정).
- 아이콘 = **C안(닥터 마스코트 통일)**. B안(AMR 초록 마크) 거부. 닥터는 마스코트지만 사용자 선택.
- Braze "프로토타입" 배지 수정 = **C안(플러그인 선언) 채택**(B안 intent-플래그는 타입마다 contracts+i18n×19 반복이라 확장 불가).

## 잔여 / Next Action
1. **Braze IAM "프로토타입" 배지 수정 (C안)** — 루트원인: `home-hero/chips.ts:157` Braze칩이 `projectKind:'prototype'` 생성 + `RecentProjectsStrip.tsx:468 projectCategory()`(+DesignsTab.tsx:1114 중복)에 braze 분기 없음 → 기본 'prototype' 라벨(`designs.tagPrototype`).
   - **C 설계**: 카드 배지를 `project.metadata.pluginId`(=바인딩 시나리오 플러그인, 예 `example-braze-iam`)가 **매니페스트에 선언한 배지**에서 도출. 매니페스트(`plugins/_official/examples/*/open-design.json`)에 배지 필드(예 `od.badge:{label,tone}`) 추가 → projectCategory가 pluginId→매니페스트 배지 조회. fallback은 기존 medium kind.
   - 효과: blog/instagram/landing 등 5종 출력타입 추가 시 **매니페스트 1줄**만. 코어/contracts/i18n 반복 없음.
   - 확인됨: ProjectMetadata에 `pluginId`(projects.ts:249)·`skillId` 저장됨 → 식별 신호 존재. 매니페스트엔 배지 필드 아직 없음(신설 필요, contracts scenario 스키마 닿음).
   - projectCategory/ProjectTag는 RecentProjectsStrip + DesignsTab 2곳 **중복** — 둘 다 수정 or 공통 추출.
   - dual-track(AGENTS.md): 카드는 UI지만, 새 매니페스트 배지 필드는 CLI `od braze`/project 출력에도 영향 가능 — 검토.
2. **시각 리브랜딩 origin 푸시** 여부 — 현재 로컬만. `fu/visual-rebrand` 푸시 or 추가작업 후 일괄.
3. (저우선) 데스크톱 아이콘 실제 패키지 반영 검증 = `pnpm tools-pack mac build`(무거움, 미실행). 형식·경로만 검증함.

## Blockers / 사람 대기
- 없음. Braze C안은 contracts(매니페스트 스키마) 닿는 기능변경 — 새 세션 권장(이번 세션 컨텍스트 200K+).

## 인프라 상태
- web dev 인스턴스 기동 중(namespace default, `127.0.0.1:57874` web / `:57873` daemon, pid 17308/17185). 내가 띄운 것 아닌 기존 것 — 재개 시 스크린샷에 재사용 가능.
- better-sqlite3=node@24 ABI137 유지.

## Uncommitted
```
(none — fu/visual-rebrand 트리 clean, 3커밋 로컬, HANDOFF.md만 untracked)
```
