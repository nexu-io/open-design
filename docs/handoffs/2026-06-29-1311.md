---
status: in_progress
position: P2 Naver-blog 산출물 — 구현+검증+designSystem 갭 fix 전부 완료. 남은 것 = PR뿐.
last_updated: 2026-06-29T04:00:17Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. PR 단계에만 발동. 재개 에이전트는 각 항목 이해를 표명한 후 진행.
- [ ] CONSTRAINT: gh push 전 `gh auth switch --user Gmin82` — origin=Gmin82/open-design, credential helper가 evan2942 캐시 → 미전환 시 403. (반복 발생, 검증됨)
- [ ] CONSTRAINT: commit/PR = Co-authored-by 등 co-author 트레일러 금지(AGENTS.md "Git commit policy"가 글로벌 위). 커밋 형식 `[what] — [why]`.
- [ ] CONSTRAINT: 재검증 필요 시 node@24 (`export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`) + typecheck 전 `pnpm --filter @marketing-ax/contracts build` (dist gitignored).

## Anti-Patterns (이번 세션에서 발견)
| 패턴 | 내용 | 심각도 | 회피책 |
|---|---|---|---|
| 지정(metadata)≠전달(컬럼) | 매니페스트 `designSystem.ref:bodoc` → apply가 `metadata.designSystemId=bodoc` 생성하나, project 컬럼엔 안 올라가고 run-start(server.ts:7581)는 컬럼/run-param만 읽음 → bodoc DESIGN.md 미주입(범용 글). braze 포함 모든 designSystem.ref 시나리오 플러그인 공통 갭. | blocking(기능 의도 깨짐) | **fix됨**(6acff6e60, 서버사이드 resolveStampDesignSystemId). 향후 시나리오 플러그인 추가 시 이 경로 확인. |
| 단일 agent 추적 과신 | Explore agent 1회가 "web이 projectMetadata.designSystemId 안 씀" 단정 → 실제 HomeView:1522서 씀(부분 오독). 라이브 실증+다중 추적으로 교정 | advisory | 결정적 주장은 라이브 ground-truth로 교차검증 |

## 현재 상태
naver-blog 산출물 **9-Task 구현 + 각 태스크리뷰 + 최종 whole-branch 리뷰(opus) + designSystem 바인딩 갭 fix + 리뷰 + 라이브 produce 실증 전부 완료.** 브랜치 `fu/p2-naver-blog` @ 6acff6e60 (11 커밋). **남은 단계 = PR뿐.** 코드/검증 끝.

## 완료 (이번 세션)
- subagent-driven-development로 플랜 Task 1~8 구현(각 implementer→task-reviewer, 전부 1-pass Approved, 수정 dispatch 0). green-sequencing 강제 준수.
- 자동 게이트: guard 63/63, repo typecheck clean, contracts 173/173, web project-card-tag 10/10, daemon naver-blog 12/12(badge·catalog·ds-bind).
- 기존 실패(web 7 + daemon chat-route 1) base 0f0f7b061서 동일 재현 → pre-existing 확정(신규 0).
- 최종 whole-branch 리뷰(opus): READY TO MERGE.
- **사용자 질문 발단 designSystem 갭 발견·실증·수정**: 칩/CLI 생성 프로젝트가 bodoc 미주입(범용 글)이던 것 → 서버사이드 fix(6acff6e60) → 라이브 produce로 bodoc 특화 글 생성 확인(§11 페르소나·서비스·금지어준수·craft준수).
- 라이브 검증: discovery 라우트·배지 pos/neg·CLI dual-track·fix ds=bodoc·실제 produce 전부 ✓.

## 잔여
1. **PR** — stale HANDOFF.md 제거 + roadmap.md 포함여부 결정 → `gh auth switch --user Gmin82` → `git push -u origin HEAD` → `gh pr create`. 템플릿 전 섹션:
   - Why: P2 2번째 버티컬(Naver blog) + 발견된 designSystem 바인딩 갭 수정.
   - What users see: 홈히어로 Naver Blog 칩 → green 네이버블로그 배지 → 인터뷰→produce가 보닥 특화 SmartEditor HTML + brief.md.
   - Surface area: Skills/Design systems(bodoc §11)/Design templates(mirror)/Craft/CLI(--plugin 재사용)/i18n(2×19)/daemon(project-routes 바인딩 fix).
   - **dual-track 명시** (칩 + od project create/run start --plugin).
   - **designSystem fix가 daemon 코드 추가**(Path A "데몬코드 0" 일부 깨짐) — 배지도 서버소유 선례라 일관, PR에 명시.
   - 캐리 Minor(최종리뷰 전부 accept-as-followup) + fix 3 Minor 비차단.
   - 칩/배지/produce 스크린샷 첨부.

## 결정 (이번 세션 — DECISIONS.md append됨)
- 실행방식 = subagent-driven(fresh implementer/task + 독립 reviewer + 최종 opus).
- designSystem 갭 = 서버사이드 resolve(배지 대칭) 채택 — web-only 미채택(CLI dual-track 깨짐). 컬럼이 단일 진실원.
- 라이브 produce로 bodoc 특화 E2E 실증(코드추적만으로 끝내지 않음).

## Blockers / 사람 액션 대기
- 없음. 게이트 = 사용자 PR 승인(outward-facing).

## 인프라 상태
- dev 런타임 정지함(stop 완료). 빌드 dist = green 상태.
- 워크플로/agent 트랜스크립트 잔존(무해).
- `.superpowers/sdd/progress.md` ledger에 nb 섹션 전 과정 기록(커밋·검증·라이브·fix).

## Uncommitted
```
 M docs/marketing-ax-roadmap.md
?? HANDOFF.md
```
(.superpowers/sdd/*, docs/handoffs/* 등은 gitignored/기존. 11 커밋은 전부 committed. roadmap.md=P2 상태 doc, PR 포함여부 미정.)

## Next Action
새 세션: `/resume-work` → 이 HANDOFF → Blocking Constraints 3건 이해 표명 → **stale HANDOFF.md 제거 + roadmap.md PR 포함여부 사용자 확인 → `gh auth switch --user Gmin82` → push → `gh pr create`(템플릿 전 섹션 + dual-track + designSystem fix 명시 + 스크린샷)**. 코드 변경 불필요 — PR만.
