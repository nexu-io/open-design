---
status: in_progress
position: P0 리브랜딩 v5 — COMPLETE (Task1~14 + 최종리뷰 + 로컬 main 머지 + origin 푸시). 후속 3건 미착수.
last_updated: 2026-06-25T23:23:48Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 재개 에이전트는 각 항목 이해를 표명한 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 모든 pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`. 시스템 node v25.8.2(ABI141) ≠ node@24(ABI137). 불일치 시 better-sqlite3 네이티브 import 실패 → daemon 테스트 대량 false-fail. (이번 세션 456-fail triage 근본원인.)
- [ ] CONSTRAINT: shell=zsh — `$PIPESTATUS` 빈값(zsh는 `$pipestatus`). 파이프 exit 필요하면 `cmd >log 2>&1; echo $?`.
- [ ] CONSTRAINT: grep=`grep -rnE`(ERE). PCRE 룩어헤드 `(?!…)` BSD grep crash/무음 금지. 보존은 `| grep -vE "$PRESERVE"` 후필터. 룩어헤드는 perl만.
- [ ] CONSTRAINT: perl `.od` 앵커 — `s{...}{...}` 구분자서 `}`가 lookahead char class 안에 있으면 **조기종료 파싱실패**. corrected class는 `s!...!...!` 구분자 + trailing class에 `;` `,` `:` `\` `)` 포함(Task7/11 실측 맹점). `.od`는 `\.od` 리터럴 dot(pod/god 보호).
- [ ] CONSTRAINT: gh 계정 — origin=`Gmin82/open-design`. push는 `gh auth switch --user Gmin82`로 active 전환 필수(evan2942로는 403). 현재 active=Gmin82.

## 현재 상태
**P0 리브랜딩(OD→Marketing AX 포크 리네임) 전부 완료.** 브랜치 main=`5f7e04121`(머지커밋), origin/main 동기화(0 ahead). 트리 clean. rebrand 브랜치 7개 삭제됨. durable ledger=`.superpowers/sdd/rebrand/progress.md`에 전 task SHA·결정·follow-up 기록 보존.

## 완료
- **Task1~14 전부** 구현+리뷰. 코드체인 Task6~9(scheme/host·데이터디렉터리·제품명·sweep), disjoint Task10/11/12/12b/12c/14(worktree 병렬→6/6 clean 머지).
- **최종 whole-branch 리뷰(opus, seam-integrity)**: 8 invariant clean, **2 seam miss 발견+수정** — 릴리스 태그 `open-design-v`→`marketing-ax-v`(`ebf52b82c`), vercel.json `@open-design/web` filter 빌드깨짐(`ef9687c62`). 둘 다 config-writer 미스윕 클래스.
- **로컬 main 머지**(`--no-ff`, `5f7e04121`) + **origin/main 푸시**(`c54e49aae..5f7e04121`, 58커밋=rebrand 30+기존 무관 27).
- 게이트: guard 63/63 · typecheck PASS · daemon 4528 pass / 1 known chat-route flake. boot smoke PASS(`<title>Marketing AX</title>`, `.max` 생성, /api/health 200).

## 잔여 (별도 PR 권장 — 의도적으로 rebrand 스코프서 분리)
1. **landing-page 전체 리브랜드** — 현재 의도 제외(odgrep/broadgrep `apps/landing-page/` 필터). 별도 P단계. `@open-design/landing-page` 패키지명·`od://app`·`open-design-v` 태그참조(github.ts:107) 등 잔존은 전부 의도.
2. **RUNTIME_DATA_DIR escape 리팩터** — 데이터루트 contract(AGENTS.md:101-160). Task7서 fallback `.od`→`.max` 최소리네임만 함(D2 결정). escape 후보 3곳(registry.ts:53 `defaultRegistryRoots`, daemon-paths.ts:135, db.ts:34 `openDatabase`)을 RUNTIME_DATA_DIR 인자경유로 교체 = 별도 follow-up. rebrand 이전부터의 아키텍처 부채.
3. **mcp-spawn conversation-binding 결함** — Task9 ADJACENT 발견. run이 seeded conversation 아닌 최근활성 conversation에 바인딩(다른 UUID). pre-existing(BASE서도 결정적 fail), rebrand 무관. red-spec 후속 PR.

## 결정 (이번 세션)
- 실행구조 = Task5 review + disjoint 6 worktree 병렬 + 코드체인 순차. 사용자 "병렬 가능 태스크 병렬".
- 사용자 결정 3건: ①od:// MCP/telemetry namespace=보존(내부계약, od bin 동일논리) ②데이터루트 .od fallback=최소리네임+escape리팩터 별도follow-up ③.od-data/.od-e2e=.max-* 리네임/.od-skills 보존.
- PR 방식=main 직푸시(PR 없이). 사유: rebrand 이미 로컬 머지+브랜치삭제, origin/main이 27 무관커밋 뒤져 깨끗한 rebrand-only PR 불가. 이미 로컬 리뷰 완료라 포크 PR 생략.

## Blockers / 사람 액션 대기
- 없음. (gh active=Gmin82 정리됨, 푸시 성공.)

## 인프라 상태
- 백그라운드 프로세스 없음. worktree `/private/tmp/p0-wt/*` 제거됨. agent worktree 2개(`.claude/worktrees/agent-*`) 무관·미접촉.
- 환경 위생: better-sqlite3=node@24 ABI137 재빌드, plugin-runtime/dist 최신(둘 다 gitignored). **유지**.
- main repo `.max/` 런타임데이터 생성됨(boot smoke 산물, gitignored).

## Uncommitted
```
(none — 트리 clean, main==origin/main)
```

## Next Action
P0 완료. 후속 착수 시:
1. `/resume-work` → Blocking Constraints 표명 + `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"` + `gh auth status`(active=Gmin82) 확인.
2. 후속 우선순위 사용자 결정: landing-page 리브랜드(가장 큼) vs RUNTIME_DATA_DIR 리팩터(contract) vs mcp-spawn red-spec. 각각 별도 브랜치+PR.
3. ledger `.superpowers/sdd/rebrand/progress.md` Read로 전체 SHA·MINOR-CARRY 맥락 복원.
