---
status: in_progress
position: Braze 리서치·도메인문서 완료 / 데이터모델 설계 게이트 직전
last_updated: 2026-06-23T04:19:03Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 재개 에이전트는 각 항목 이해를 표명한 후 진행.
- [ ] CONSTRAINT: REST로 IAM 전송 불가 — Braze는 인라인 `in_app_message` 객체 스키마를 어떤 REST 엔드포인트에도 공표 안 함(2차 리서치 3-0 검증). IAM 콘텐츠는 대시보드 작성, 트리거는 클라이언트 SDK 커스텀이벤트만. 회피책: `braze_iam_v1`을 JSON 페이로드로 설계하지 말 것 — **Custom HTML 아티팩트**로. 프로퍼티어리 IAM JSON fabricate 금지. 근거 BRAZE-DOMAIN §4.
- [ ] CONSTRAINT: bodoc 데이터 = 도메인 입력만, 코드출처 아님 — BRAZE-BODOC-CATALOG.md는 보닥 실데이터(PII/내부값 가능). 외부공유·커밋 공개 주의. bodoc은 타당성 레퍼런스(DECISIONS 2026-06-23). 회피책: 카탈로그를 craft/질문폼 입력으로만 쓰고, 보닥 식별자(bodoc://, 보닥명)를 M-AX 고유 정체성으로 오인 금지.
- [ ] CONSTRAINT: §3.1 route-registry 추출 ↔ 확정 P1 충돌 (직전 세션 이월) — ARCHITECTURE-BRAZE §3.1 "route-registry 추출"은 확정 P1(startChatRun+shouldRunReview 리팩터만)과 어긋남. 이미 reconcile 노트 박음(§6-7). 회피책: 구조개선 제안 전 DECISIONS/ENGINE-BRAIN-SEAM 확정사항 확인.

## 현재 상태
M-AX(Marketing AX) 포크 = OD를 브랜드 마케팅 크리에이티브 워크스페이스로. 이번 세션: Braze 타깃 도메인 리서치 2회(deep-research 스킬) + 보닥 실 카탈로그 파싱 완료. 핵심 발견으로 제품 모델이 바뀜(REST IAM 전송 불가 → Custom HTML 아티팩트+대시보드 핸드오프). **경로 1 확정.** 다음 = 데이터모델 설계(코드 전 게이트).

## 완료
- **BRAZE-DOMAIN.md** 신규(7섹션): §1 IAM 유형·이미지스펙, §2 Custom HTML 제약+brazeBridge JS API, §3 REST(엔드포인트/인증/send·schedule/Templates), §4 ⚠️인라인 IAM 객체 없음(대시보드 모델), §5 세그/트리거/전달, §6 잔여 Open Q, §7 소스인덱스. 리서치 2회 3-0 검증(1차 25claim·0kill / 2차 24claim·1kill).
- **BRAZE-BODOC-CATALOG.md** 신규: 보닥 xlsx 2종 파싱 → 128 커스텀이벤트(+프로퍼티/타입/시점), 122 어트리뷰트, 13 딥링크. PII 예시값 제외.
- **ARCHITECTURE-BRAZE.md** reconcile: §1.5/§2.1/§6/§11 = IAM JSON 모델 → Custom HTML 아티팩트 모델로 수정.
- **DECISIONS.md** 5건 append (스코프 A·아키텍처A 재확인·통합모델전환·카탈로그·경로1).
- 직전 HANDOFF 아카이브 → docs/handoffs/2026-06-23-1236.md.

## 잔여
- 데이터모델 설계(게이트): `braze_messages` 테이블 컬럼 확정 + `live-artifacts/schema.ts` `braze_iam_v1`(=Custom HTML) format 정의 + 디스커버리 질문폼 항목(트리거이벤트=카탈로그 128 중 / 어트리뷰트 세그 / 전달모델).
- 모듈 구현(dual-track): contracts DTO → daemon 라우트+마이그레이션 → web UI → `od braze` CLI → design-templates/braze-iam/SKILL.md + craft 룰(brazeBridge/logClick/이미지스펙/딥링크).
- BRAZE-DOMAIN §6 Open Q 5건(SDK IAM 필드 정밀스키마는 HTML경로라 불요 가능 / HTML·CSS 샌니타이징은 craft 확정 전 확인 권장).
- P0 리브랜드 = 별도 트랙, 기능 안정화 후.

## 결정
(전부 DECISIONS.md 반영 완료)
- 스코프 = (a) 전체 Marketing AX. 아키텍처 = A 단일 마케팅 두뇌(engine/brain 분리 폐기 유지).
- "디자인" 2의미 구분: 크래프트(마케팅두뇌 흡수) vs OD 제품두뇌(②만 BrainProvider 필요). 카드뉴스/IAM 시각작업=①.
- Braze 통합 = **경로 1**: Custom HTML IAM 아티팩트 생성 + 대시보드 핸드오프. braze-client REST(경로2)·Content Card(경로3) 보류.

## Blockers / 사람 액션 대기
- **데이터모델 게이트(사용자 리뷰)**: braze_messages 스키마 + braze_iam_v1 정의를 다음 세션에서 설계 후 사용자 리뷰 받고 코드 착수(재작업 방지).
- Braze 계정/API키: 경로1은 불필요. 경로2(REST) 보류라 당장 안 막힘.

## 인프라 상태
- 백그라운드 프로세스 없음. 데몬/web 미실행. 리서치 워크플로우 2건(wgakw3814/w9a4fkvmm) 완료·종료.

## Uncommitted
```
 M ENGINE-BRAIN-SEAM.md
 M docs/superpowers/plans/2026-06-22-p1-engine-brain-seam.md
 M docs/superpowers/specs/2026-06-22-marketing-ax-product-design.md
?? ARCHITECTURE-BRAZE.md
?? BRAZE-BODOC-CATALOG.md
?? BRAZE-DOMAIN.md
?? DECISIONS.md
?? FORK-DELTA.md
?? docs/handoffs/
```
(M 3개 + FORK-DELTA = 이전 세션 변경분. 이번 세션 신규 = BRAZE-DOMAIN.md, BRAZE-BODOC-CATALOG.md + ARCHITECTURE-BRAZE.md/DECISIONS.md 수정.)
⚠️ BRAZE-BODOC-CATALOG.md = 보닥 PII 가능 → 커밋/공개 전 점검.

## Next Action
1. 데이터모델 설계: `apps/daemon/src/db.ts` 현 테이블 패턴 Read → `braze_messages` 컬럼안(brand_id, output_mode, artifact_path, braze_campaign_id?, delivery_channel, status) 확정.
2. `apps/daemon/src/live-artifacts/schema.ts`(또는 해당 경로) Read → `braze_iam_v1` = Custom HTML 아티팩트 format 정의(html_template_v1 기반).
3. 디스커버리 질문폼 항목 초안: 트리거이벤트(BRAZE-BODOC-CATALOG §3에서 선별) / 세그먼트 어트리뷰트(§2) / 전달모델(트리거 vs 스케줄).
4. 위 3종 = **사용자 리뷰 게이트** 후 코드 착수(dual-track: contracts→daemon→web→CLI→SKILL.md).
