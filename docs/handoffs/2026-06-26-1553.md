---
status: in_progress
position: P1(startChatRun 정리) 출하 완료 — PR#4 머지. 다음 = P2 마케팅 도메인, 사용자 착수 지시 대기.
last_updated: 2026-06-26T05:12:42Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약. 재개 에이전트는 각 항목 이해 표명 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 모든 pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`. 시스템 node25(ABI141)≠node@24(ABI137) → better-sqlite3 실패.
- [ ] CONSTRAINT: gh push 전 `gh auth switch --user Gmin82` 명시 — credential helper가 evan2942 캐시 → 403. origin=Gmin82/open-design. (이번 세션 재확인, 정상 동작.)
- [ ] CONSTRAINT: `pnpm typecheck` 전 `pnpm --filter @marketing-ax/contracts build` — contracts dist gitignored, stale면 TS2305/2339.
- [ ] CONSTRAINT: zsh — 파이프 exit는 `$pipestatus`(소문자). `$PIPESTATUS` 빈값.
- [ ] CONSTRAINT: 테스트 env 'default' 디자인시스템 brand(registryBody) 미해석 — daemon이 RUNTIME_DATA_DIR서 DS 해석, 임시 데이터루트엔 'default' DESIGN.md body 미시드 → `critiqueBrand` undefined. critique 패널이 *출현하는* route-level e2e positive 작성 시 brand fixturing 필요(이번 세션 e2e positive 제거 사유). composer positive는 critique-composer.test.ts가 brand 직접 주입으로 커버.
- [ ] CONSTRAINT: i18n 키 추가 시 18~19 로케일 전부 + types.ts Dict.

## 현재 상태
**P1 = 출하 완료.** PR #4 → origin/main 머지(`414be2ea6`). 로컬 main 동기화, 브랜치/worktree 정리, 트리 clean(HANDOFF.md만 신규).

P1 정찰 결과 = 구조 ~90% 상류 충족 → 잔여(인라인 5항 eligibility를 명명 헬퍼 `shouldRunReview()`로)만 구현. 동작 100% 보존, 32케이스 truth table + 적대적 등가검증 PASS.

## 완료 (이번 세션)
- **로드맵 인프라**: `docs/marketing-ax-roadmap.md`(tracked 진실원, P0~P3+산출물5종 상태표) + 메모리 `marketing-ax-roadmap.md` 생성. 매 세션 자동 환기. commit `b211eb18c`.
- **P1 (shouldRunReview)**: 정찰 워크플로(5에이전트)로 P1이 상류 머지(`3fb849d04` prompt-builder 추출 / `bb2015766a` Phase 5 lockstep 단일소스)로 이미 ~90% 충족 발견. 잔여 = `critique/rollout.ts`에 순수 `shouldRunReview(ReviewRunEligibility)` 추가(불변식 docblock) + server.ts:7927 인라인 치환(소비 3곳 무변경) + truth table 32케이스. PR#4 머지(`8f8e908a4`).
- 검증(3병렬 워크플로): guard 63/63 · typecheck clean · critique 255pass/0fail · 적대적 동작-등가 divergence 0. 회귀 0(OPENCODE_CONFIG는 clean main서도 실패=기존결함, baseline diff 확인).

## 잔여 (전체 포크 — 별도 결정/착수 대기)
**다음 메이저 = P2 마케팅 도메인** (산출물 5종 중 Braze IAM만 구현, 나머지 4종 미설계):
- 크리에이티브 기획 · **블로그(네이버블로그)** · **SNS(인스타 카드뉴스)** · 랜딩페이지 = spec/plan 0건
- 권장 착수 = B안(인스타 카드뉴스 or 네이버블로그 1종 슬라이스, Braze IAM 패턴 복제 — 칩→인터뷰→produce→brief.md→배지). 스펙이 "2번째 버티컬 등장 시" P2 추상화 결정 미뤄둠 → 슬라이스가 그 트리거.
- 상세: `docs/marketing-ax-roadmap.md`.

**P3 커스텀 검수** (핵심 차별점) = 미설계.

**저우선 백로그**: Braze 후속(ProjectTag dedup · kanban 배지 · 기존 2건 stale) · 리브랜드 후속(landing 패키지명·env · blog/tutorial 본문 · RUNTIME_DATA_DIR escape 리팩터 · 데스크톱 아이콘 실빌드) · startChatRun 2921줄 god-function 분해(P1 범위 밖).

## 결정 (이번 세션 — DECISIONS.md append됨)
- P1 정찰 선행 → "잔여만 구현"(사용자 AskUserQuestion). close-as-done/god-function 공략 아님.
- shouldRunReview = critique/rollout.ts co-locate(isCritiqueEnabled 옆), 순수함수, 불변식 docblock.
- route-level e2e positive 제거(brand fixturing 취약) → lockstep은 truth table+composer+opt-out+spawn-wiring로 잠금.
- P1 통합 = PR#4 생성+머지(braze PR#3 패턴).

## Blockers / 사람 액션 대기
- 없음. 다음 = P2 착수 산출물 선택(인스타 카드뉴스/네이버블로그/기타) 사용자 지시 대기.
- (참고) 앱 채팅 dogfooding 시 모델 이슈: 글로벌 ~/.claude/settings.json model=sonnet[1m] fallback → 컴포저서 표준모델 선택 필요(코드 무관).

## 인프라 상태
- dev 런타임 미기동(이번 세션 코드/테스트만). 기동: `pnpm tools-dev run web --daemon-port 17456 --web-port 17573`.
- contracts dist 빌드됨. better-sqlite3=node@24 ABI137.
- 워크플로 트랜스크립트 잔존(무해): `.../subagents/workflows/wf_67e7208f-a63`(p1-recon), `wf_4b84aa39-d22`(p1-verify).

## Uncommitted
```
(none — main=origin/main 동기화, clean. HANDOFF.md만 신규)
```

## Next Action
사용자 P2 착수 지시 대기. 권장 = 산출물 1종 슬라이스(인스타 카드뉴스 or 네이버블로그) 선택 → brainstorming → SDD spec/plan → Braze IAM 패턴 복제 구현.
재개 시 먼저: `docs/marketing-ax-roadmap.md` 상태표 + 이 HANDOFF Blocking Constraints 확인.
