---
status: in_progress
position: P0 리브랜딩 플랜 v3 → v4 개정 대기 (3자 적대리뷰 완료, 픽스목록 확정)
last_updated: 2026-06-24T06:32:01Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 각 항목 이해 표명 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 시스템 node 충돌. 모든 pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`.
- [ ] CONSTRAINT: **grep BRE vs ERE** — 플랜의 `odgrep`/`broadgrep` = `grep -rn`(BRE)서 `|` 는 리터럴(alternation 아님). 다중패턴 sweep 전부 `grep -rnE` 또는 `-e` 필요. (v3 게이트 거짓통과 근본원인 — v4가 반드시 수정.)
- [ ] CONSTRAINT: perl 치환 한줄 = **shell-safe + regex-correct 둘 다** 검증해야. v3 Task7서 미이스케이프 `.` + 노출 백틱 둘 다 터짐. perl 스크립트는 단일쿼트 변수/파일로, dot은 `\.`.

## 현재 상태
**P0 리브랜딩(OD→Marketing AX) 플랜을 3자 적대리뷰(Claude 인라인 + plan-reviewer opus 서브에이전트 + codex 0.135.0)했다. 셋 다 "v3 실행 불가" 판정.** v3는 v2 스코프갭은 고쳤으나 **내가 넣은 실행버그**(정규식 미이스케이프·BRE alternation·shell-unsafe perl) + 잔여 스코프갭(helm/compose·데이터루트 contract). v4 개정 필요. 픽스목록 아래 확정.

플랜 파일(gitignored, 디스크 영속, 직접 Read):
- v3: `docs/superpowers/plans/2026-06-24-p0-rebranding-v3.md` (13 task)
- v2: `docs/superpowers/plans/2026-06-22-p0-rebranding.md` (참조)
- 리뷰 산출물: codex `scratchpad/codex-out.txt`, 서브에이전트 결과는 본 HANDOFF에 인라인.

## 완료 (이번 세션)
- **bodoc DS 포팅 Task 3 e2e** — browse 헤드리스 produce 완주, variant/brief grep PASS, 시각확인 PASS, 검증게이트 PASS. main 머지(19ffeaacb, 26커밋 ff) + 브랜치 삭제. (DECISIONS 기록됨)
- **P0 플랜 v3 작성** — v2 이중리뷰 반영(Task7 앵커·C-2 충돌스캔·스코프확장 Task10~12·nix해시·테스트게이트). 
- **v3 3자 리뷰** — 아래 픽스목록이 결과.

## 잔여 — v4 개정 픽스목록 (3리뷰 합본, 우선순위순)
### Critical (실행버그 — 반드시)
1. **Task7 한줄 이중버그**: (a) `.od`→`\.od` 이스케이프(미이스케이프 시 `'pod'`→`'.max'` 231사이트 손상: comments.ts·srcdoc.ts·FileViewer.tsx) (b) 브래킷 백틱 제거(Bash 명령치환 crash). perl을 단일쿼트 파일/변수로.
2. **grep 헬퍼 BRE→ERE**: `odgrep`/`broadgrep` `grep -rn`→`grep -rnE`. 안 고치면 Task9 Step1·Task13 Step1/2 다중패턴 sweep + Task7 Step1 추출이 **0 거짓반환** → Task7 무음 no-op·게이트 공허통과. (검증: `broadgrep 'a|b'`→0 vs `'a'`→547). Task7 Step1 ERE 재작성 + `[:space:]`→`[[:space:]]`.
3. **스코프 추가**: `tools/pack/helm/open-design/`(Chart.yaml·_helpers.tpl) + `tools/pack/docker-compose.yml` (stale 80). Task12 or 신규태스크. Task13 broadgrep에 `tools/pack` 포함.
4. **데이터루트 contract**: Task7 Step5 escape후보 `daemon-paths.ts:135`·`db.ts:34`·`plugins/registry.ts:53`를 `RUNTIME_DATA_DIR` 경유 강제(문자열 rename 불충분, AGENTS.md:119-125).
5. **문구정정**: "validate.ts 앵커 채택" 거짓(실제 좌경계 다름, 이웃보호는 작동) → 정확히 기술.

### High
6. **Task11/12 "Open Design"(대문자) 치환 추가** — nix 17·deploy 33 (nix/package-daemon.nix:187·deploy/aws/template.yaml:2,534·install.sh:119). Step3 residual 체크에도 추가.
7. **nix/deploy `.od` 데이터디렉터리 rebrand** — Task7 코드스코프만이라 nix/home-manager.nix:28·module-common.nix:7,62·deploy/docker-compose.yml:23·Dockerfile:103·azure bicep·aws template·uninstall.sh:132 잔존. Task11/12에 `.od`→`.max` 추가 + Task13 broadgrep에 `.od` 포함.
8. **AGENTS.md:145 릴리스채널 정책 모순** — stable="Open Design" 강제 ↔ Task8 "Marketing AX". *.md 제외라 stale. 같은 플랜서 AGENTS.md 갱신 or 메인테이너 waiver 결정.
9. **Task13 sweep 확장** — helm/compose + `scripts/install-unsafe-dmg.sh:88` 포함.

### Medium
10. v3 거짓주장 정정: "OD_MAX_* 부재"→**존재**(app-config.ts:70·pipeline.ts:7,40; perl이 MAX_MAX_… 생성, self-consistent 무해하나 주장 틀림. v2는 맞았음).
11. "Open Design Beta=0"→**67건**(테스트 파일; production identity.ts는 파생 OK이나 Task8 Step1 게이트 문구 틀림).
12. `guard.ts:50` bare `.od` Step4에 추가(현재 :525만).
13. Task7 = 177줄/100파일 → blind xargs 금지, 큐레이트 reviewed 파일목록.
14. `.od-data` e2e/툴링(playwright.config.ts:7·e2e/tsconfig.json:35·guard.ts) 수용규칙 명시("결정" 모호).
15. Task2 충돌은 typecheck break 아님(string key vs symbol) → 하드블로커 아닌 선택해소로 격하.

### 3자 합의 — 정확(유지)
collision=1건 · `\bOD_(?!MOCKS_)` 정확·OD_MOCKS_ 보존(mocks-golden.test.ts:64만) · nix:update-hash 존재 · Task10/11/12 카운트·인용 정확 · 순서/병렬 OK · AGENTS 데이터contract 위반 없음.

## 결정 (이번 세션)
- bodoc Task3 e2e PASS → main 머지(로컬, 26커밋 ff) + 로컬브랜치 삭제. 원격 미푸시.
- P0 플랜 리뷰 = 3자 적대(인라인+서브에이전트+codex). 각기 다른 버그계층 포착(스코프/쉘안전/정규식정확성) — 멀티리뷰 가치 입증. v4 필요.
- 거짓주장 3건(validate.ts채택·OD_MAX_부재·Beta=0)은 내 v3 작성 오류 — v4서 정정.

## Blockers / 사람 액션 대기
- **v4 개정 = 다음 세션 첫 작업** (현 세션 컨텍스트 ~230K 소진, 큰 Write 위험해 퍼즈).
- AGENTS.md 릴리스채널 정책(High #8): 갱신 vs waiver = 메인테이너 결정 필요(사용자).
- main 26→27커밋: 사용자 본인 병렬커밋 bf7047292(braze-plan-copy, title/body copy) 15:31 유입 — 정상, 내 작업 무관. 미푸시 유지.

## 인프라 상태
- dev 런타임 살아있음: web :62911 / daemon :62910 / node@24 / namespace default. browse 데몬 살아있음(PID 61740, 현재 file:// variant 렌더 상태).
- bodoc produce 테스트프로젝트 `3c2f042d` (.od/projects/, gitignored 런타임).

## Uncommitted
```
 M .gitignore     # gstack 툴링 — 무관, 커밋 제외
 M DECISIONS.md   # bodoc Task3 + P0 v3리뷰 결정 append됨
# 주의: docs/superpowers/plans/2026-06-{22,24}-p0-rebranding*.md = gitignored, 미표시. 디스크 존재. v2·v3 직접 Read.
# main: bodoc 4커밋 + 사용자 braze커밋 적재. origin 미푸시(27 ahead).
```

## Next Action
1. `/resume-work` → Blocking Constraints 3건(node@24·grep BRE/ERE·perl shell+regex) 이해 표명.
2. v3 Read → 위 픽스목록(Critical 1~5, High 6~9, Medium 10~15) 반영해 **v4 작성**: `docs/superpowers/plans/2026-06-24-p0-rebranding-v4.md`.
3. v4 핵심: 모든 sweep `grep -rnE`, Task7 perl `\.od` 단일쿼트파일, helm/compose+nix/deploy `.od`+"Open Design" 스코프, 데이터루트 RUNTIME_DATA_DIR 경유, AGENTS 정책 결정 반영, 거짓주장 3건 정정, Task7 큐레이트 파일목록.
4. (선택) v4 작성 후 다시 codex/서브에이전트 1회 재검증 — 특히 정규식/쉘 한줄들.
5. v4 합의 후에야 실행(Task1부터). 실행 자체는 또 다른 세션 권장(1000파일 순차치환).
