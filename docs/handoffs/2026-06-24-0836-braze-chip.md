---
status: in_progress
position: Braze IAM feature 완료 + PR #1 오픈 / 리뷰·스크린샷·후속 대기
last_updated: 2026-06-23T06:24:11Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 재개 에이전트는 각 항목 이해를 표명한 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 시스템 기본 node가 v25라 `pnpm install`/typecheck/test/better-sqlite3 빌드가 깨짐. 모든 pnpm/node 명령 앞에 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"` 필요. 회피책: 빌드/검증 셸마다 PATH prefix.
- [ ] CONSTRAINT: REST로 IAM 전송 불가 — Braze는 IAM 콘텐츠를 어떤 REST 엔드포인트로도 안 받음(BRAZE-DOMAIN §4). `braze_iam_v1`은 JSON 페이로드 아닌 **produced HTML 파일**(craft lint 검증). 대시보드 수동 핸드오프. 프로퍼티어리 IAM JSON fabricate 금지.
- [ ] CONSTRAINT: bodoc 카탈로그 = PII, 커밋 금지 — `BRAZE-BODOC-CATALOG.md`는 `.gitignore`됨(이번 세션 추가). 도메인 레퍼런스 입력 전용. 보닥 식별자(bodoc://, 보닥명)를 M-AX 정체성으로 쓰지 말 것. skill/craft는 브랜드-애그노스틱 유지(bodoc=예시 브랜드, design-systems/<brand>로 파라미터화).

## 현재 상태
**Braze IAM 제작 기능 = dual-track 완성 + PR #1 오픈.** (https://github.com/Gmin82/open-design/pull/1)
SDD(subagent-driven-development)로 실행: 6 task 전부 구현+리뷰+수정, 9 커밋. `guard` 0 / repo `typecheck` 0. 마지막 통합 리뷰(opus)가 skill↔CLI 드리프트 블로커 잡아 수정 완료.

작업 디렉토리 구조: `~/Project/open-design`(M-AX 작업본) / `~/Project/open-design-original`(clean main 순정 레퍼런스). 워크트리 안 씀.

## 완료
- **백엔드** (588d78b): contracts `braze.ts`(BrazeMessage/Variant/Plan), daemon `braze/persistence.ts`(braze_messages/braze_variants, migrateBraze) + `braze-routes.ts`(`/api/braze/*`, interview 상태가드).
- **CLI** (15dbd07+f793396): `od braze` 10 서브커맨드, --json/--plan-file, `braze-cli-args.ts` 헬퍼 + 14 test. 리뷰 Approved.
- **skill+craft** (ff1a527+0122b75+c1a456d): `design-templates/braze-iam/SKILL.md`(iam-builder 7단계 OD 포팅, question-form 인터뷰 4축) + `craft/braze-custom-html.md` + example.html + references 4종. 리뷰 Approved.
- **web UI** (0f13c8a+b957810): `apps/web` TasksView `BrazeSection`(목록/상세/variant 미리보기/삭제), 25 test, i18n braze.* 19 로케일.
- **설계문서** (03ae288): ARCHITECTURE-BRAZE/BRAZE-DOMAIN/DATA-MODEL-BRAZE/DECISIONS/FORK-DELTA + .gitignore(PII) 커밋.
- PR #1 오픈 (base main ← marketing-ax/braze-iam).

## 잔여
- **PR #1 스크린샷**: UI 서피스 체크됨 → 스크린샷 필요(헤드리스라 미첨부). 진입점 = Tasks/Automations 뷰 → "Braze IAM" 섹션. 실행 인스턴스에서 캡처.
- **PR 리뷰 피드백** 대응.
- **후속(보류) — 트리거 옵셔널화**: daemon `/interview`가 `triggerEvent`/`deliveryModel` 필수인데 검증된 워크플로우는 트리거=콘솔 설정(플랜 후보). 현재 skill이 추론 후보 전달로 우회. contract+daemon+CLI에서 optional로 완화 = follow-up(DATA-MODEL §5.1).
- **P0 리브랜드 트랙**: 별도. 기능 안정화 후(docs/superpowers/plans P0 v2).

## 결정
- 작업 디렉토리 = 현재 작업본 유지 + open-design-original(clean main) 분리. 워크트리 불요(단일 브랜치 순차).
- 데이터모델 게이트 5건 확정(추천 디폴트): 아티팩트=produced-file / message↔conv=대화당N / brand=design_system 상속+override / 전달=경로1만 / Variant=자식테이블.
- 제작 플로우 = 요청→인터뷰(4축)→기획안→컨펌게이트→제작(A/B)→수정→완료. 인터뷰/컨펌=question-form.
- skill = iam-builder 워크플로우 OD 포팅하되 브랜드-애그노스틱(브랜드 사실은 design-systems로 분리).
- 실행 = SDD + TDD + 독립 task 병렬(no-commit 후 컨트롤러 커밋, worktree 대신 디스조인트 파일).

## Blockers / 사람 액션 대기
- PR #1 스크린샷 = 사람이 실행 인스턴스에서 캡처(헤드리스 불가).
- Braze 계정/API키: 경로1(HTML+대시보드)은 불필요. 경로2(REST) 보류라 미차단.

## 인프라 상태
- 백그라운드 프로세스 없음(서브에이전트 전부 종료). 데몬/web 미실행.
- node_modules 설치됨(node@24). 빌드/검증 시 node@24 PATH 필수.
- 브랜치 marketing-ax/braze-iam = origin push됨, PR #1 연결.

## Uncommitted
```
(clean — BRAZE-BODOC-CATALOG.md만 .gitignore로 미추적)
```

## Next Action
1. PR #1 스크린샷 첨부(Tasks 뷰 "Braze IAM" 섹션) — 실행: `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; pnpm tools-dev run web --daemon-port 17456 --web-port 17573` 후 /browse 캡처.
2. PR 리뷰 피드백 대응.
3. (선택) 트리거 옵셔널화 follow-up: BrazeInterviewRequest triggerEvent/deliveryModel optional + daemon 가드 완화 + CLI --trigger/--delivery optional + skill 갱신.
4. (별도 트랙) P0 리브랜드 착수.
