---
status: in_progress
position: Braze 카드 배지 출하 완료(PR#3 머지). 다음 작업 미정 — 사용자 지시 대기.
last_updated: 2026-06-26T04:18:20Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약. 재개 에이전트는 각 항목 이해 표명 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 모든 pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`. 시스템 node25(ABI141)≠node@24(ABI137) → better-sqlite3 네이티브 실패.
- [ ] CONSTRAINT: gh push — `gh auth status` active=Gmin82여도 git credential helper가 evan2942 캐시 → 403. push 전 **`gh auth switch --user Gmin82` 명시 실행** 필수(이번 세션 재확인). origin=Gmin82/open-design.
- [ ] CONSTRAINT: `pnpm typecheck` 전 `pnpm --filter @marketing-ax/contracts build` — contracts dist는 gitignored. src 타입 추가 후 dist 미빌드면 web/daemon이 stale dist resolve → TS2305/2339. (이번 세션 typecheck blocker였음.)
- [ ] CONSTRAINT: shell=zsh — 파이프 exit는 `$pipestatus`(소문자) 또는 `cmd; echo $?`. `$PIPESTATUS` 빈값.
- [ ] CONSTRAINT: i18n 키 추가 시 19 로케일 전부 + types.ts Dict. (배지는 의도적 non-i18n freeform이라 무관.)

## 현재 상태
**Braze 카드 배지 기능 = 출하 완료.** PR #3 → origin/main 머지(`553bc42bc`). 로컬 main 동기화, 브랜치/worktree 전부 정리. 트리 clean(HANDOFF.md만 untracked).

직전: 사용자가 앱 채팅 전송 시 "Usage credits required for 1M context" 에러 → 조사 완료(아래 결정). 사용자가 fix 거부("아니야"), 퍼즈 요청.

## 완료 (이번 세션)
- **Braze 카드 배지 기능 (9태스크, SDD 병렬)**: 매니페스트 `od.badge:{label,tone}` 스키마(contracts) → 데몬 pre-insert 스탬프(project-routes, 모든 생성경로·resolve 무관) → 카드 렌더(badge>category, TONE_CLASS 룩업=CSS주입차단) → CLI `od project list` 라벨. Braze="In-App Message" pink. 서버소유(PATCH 보존, 클라 override 차단).
- 게이트: guard 63/63 · typecheck PASS · contracts 170/170 · daemon 신규 8/8 · web 신규 8/8. 시각확인(핑크 카드 스크린샷). opus 최종리뷰 READY.
- 리뷰서 잡은 것: codex/plan-reviewer가 HANDOFF 전제(pluginId 저장) 오류+post-resolve seam → pre-insert 재설계로 Critical 3개 사전제거. lane 리뷰서 PATCH override Critical 수정.
- spec/plan: `docs/superpowers/specs|plans/2026-06-26-braze-card-badge*` (gitignored 작업문서).

## 잔여 (badge 후속, 저우선·별도 결정)
- 기존 Braze 2건 stale("Prototype" 유지, 백필 안 함 — 의도)
- ProjectTag 컴포넌트 dedup defer(현재 2파일 중복, projectCategory 로직만 공유화)
- DesignsTab ProjectTag 시그니처 비대칭({project} vs union) — dedup 시 동기화
- kanban 뷰 카드 배지 미적용(범위 밖)
- 데스크톱 패키지 아이콘 실빌드 검증(이전 리브랜드 핸드오프 잔여, 미실행)

## 결정 (이번 세션)
- "Usage credits 1M context" 에러 = **앱 버그 아님**. 앱 모델="default" → 데몬이 `--model` 안 넘김(claude.ts:62 `options.model!=='default'`일 때만) → 스폰 claude가 글로벌 `~/.claude/settings.json` `model:"sonnet[1m]"` fallback → 1M context credits 게이트. 수정 3안(A 앱서 표준모델 선택=추천 / B claude.ai/settings/usage credits / C 글로벌 settings [1m]제거=전역영향 비추천). 사용자 거부 → 미적용.
- PR 방식 = visual-rebrand처럼 PR#3 생성 후 머지(로컬 직머지 아님). 사용자 "PR하고 머지해".

## Blockers / 사람 액션 대기
- 없음. 다음 작업 사용자 지시 대기.
- (참고) 앱 채팅 쓰려면 모델 이슈 위 A안(컴포저 드롭다운 default→Sonnet/claude-sonnet-4-5) 선택 필요. 코드 무관.

## 인프라 상태
- dev 런타임 **정지함**(stop 완료). 재기동: `pnpm tools-dev run web --daemon-port 17456 --web-port 17573`.
- browse 서버 정지. better-sqlite3=node@24 ABI137.
- 검증용 테스트 프로젝트 "Braze IAM Visual" .od dev 데이터에 잔존(무해).

## Uncommitted
```
(none — main clean, HANDOFF.md만 untracked. spec/plan은 docs/superpowers gitignored)
```

## Next Action
사용자 다음 작업 지시 대기. 환경 준비됨(main=origin/main 동기화, clean).
앱 dogfooding 재개 시: dev 런타임 기동 → 모델 드롭다운 표준모델 선택(1M 회피) → 채팅.
