---
status: in_progress
position: bodoc 디자인시스템 포팅 — 설계+스펙 완료, 구현 0/3. 다음 = writing-plans → 구현
last_updated: 2026-06-24T02:30:54Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 재개 에이전트는 각 항목 이해를 표명한 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 시스템 node v25라 pnpm/typecheck/better-sqlite3 깨짐. 모든 pnpm/node 명령 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`.
- [ ] CONSTRAINT: od CLI·daemon 둘 다 `apps/daemon/dist/cli.js`서 구동 — daemon src 변경 후 반드시 `pnpm --filter @open-design/daemon build` + 런타임 재기동해야 반영.

## 현재 상태
**새 트랙 = bodoc 디자인시스템 포팅. 설계+스펙 작성·self-review 완료. 구현 미시작.**
선행 트랙(brief.md 저장)은 전부 완료·push됨(PR #1). 이번 트랙은 그 위에서 발견된 후속 결함 수정.

**스펙 위치**: `docs/superpowers/specs/2026-06-24-bodoc-design-system-design.md` (gitignored — git 미추적, 디스크 영속. resume서 직접 Read 필수).

## 문제 (확정, 증거 있음)
IAM 제작이 보닥 브랜드 컨텍스트 **없이** 수행됨. 두 산출물 모두:
1. HTML produce — `variant-a.html`이 제네릭 인디고(`#4f46e5`) 발명, 보닥 `#16C5FF` 미사용, `bodoc://` 딥링크 0.
2. brief 저작 — 기획안이 `yourapp://signup` 플레이스홀더(실제 `bodoc://action/Login?method=kakao` 아님).

**근본원인**: daemon은 활성 DS의 DESIGN.md를 produce 프롬프트에 자동 stack(`apps/daemon/src/prompts/official-system.ts:26`, `panel.ts`, `discovery.ts:180`)하나, `design-systems/`에 bodoc DS 없음. 보닥 사실(딥링크13·어트리뷰트122·이벤트128)은 `BRAZE-BODOC-CATALOG.md`(gitignored, PII)에만 고립. braze 칩 config `od.context.designSystem={primary:true}`인데 braze 프로젝트 primary 없어 브랜드-블라인드.

**소스 자재(존재)**: `/Users/gyumin/Project/braze-iam/brand/{DESIGN.md(8.2KB 전섹션·실토큰#16C5FF·Pretendard),tokens.css,tokens.json,braze-fonts.css}` — 그 프로젝트서 TRACKED·저민감. 딥링크13은 `BRAZE-BODOC-CATALOG.md §1`.

## 완료 (이번 세션)
- **선행 brief.md 트랙 마무리**: web "View brief" 링크 시각확인 PASS(Tasks→Braze IAM→메시지 fc499f31→상세→View brief→FileViewer brief.md 렌더). briefPath API 정합·raw fetch 200. 500 2건은 `/api/amr/models`만(braze 무관). 이전 HANDOFF 아카이브됨(`docs/handoffs/2026-06-24-1117.md`).
- **신규 트랙 설계 brainstorming**: 근본원인 코드 추적 확정, 소스 자재 발견, 옵션 A 사용자 승인.
- **스펙 작성+self-review**: C1 DS폴더 생성 / C2 칩 bodoc pin / C3 검증+정리. dual-track·out-of-scope·검증게이트 명시.

## 잔여 (구현 — 새 세션)
1. **writing-plans 스킬**로 구현 플랜 작성(스펙 기반).
2. **C1**: `design-systems/bodoc/` 생성 — braze-iam/brand/ 4파일 포팅 + DESIGN.md에 §딥링크 카탈로그(13) 추가. `pnpm guard`(토큰 스키마 위반 0) 확인.
3. **C2**: `plugins/_official/examples/braze-iam/open-design.json`의 `od.context.designSystem`을 `{primary:true}`→bodoc `{ref}` 변경. `apply.ts` designSystemId 해소 확인. 플러그인 validate 테스트.
4. **C3**: 재제작 e2e(production HTTP/CLI) — variant HTML `#16C5FF`+`bodoc://`/인디고 부재 + brief.md 실 딥링크/플레이스홀더 부재 grep. 시각확인. 테스트잔여 brief.md("갱신본/2차 저장") 정리.

## 결정 (이번 세션 — DECISIONS.md append)
- 컨텍스트 범위 = **옵션 A**: bodoc DESIGN.md = 시각브랜드+보이스+Liquid룰+§딥링크13(committed). 어트리뷰트122/이벤트128 벌크는 제외(PII), 최소 개인화(name 등)만. 딥링크는 레퍼런스 HTML에 이미 노출된 앱 라우트라 저민감.
- 칩 자동 디폴트 = 채택: 칩 config designSystem을 bodoc ref로 pin(manifest 스키마 `{ref?,primary?}` 지원 확인 manifest.ts:189). 칩 누르면 bodoc 자동 활성, 수동선택 불요.
- DS 발견 = 자동스캔(`listDesignSystems` readdir, design-systems.ts:256) — 폴더 드롭만으로 드롭다운 노출, 레지스트리 편집 불요.
- bodoc 갭이 HTML produce뿐 아니라 brief 저작에도 적용 — Component 1(DESIGN.md §딥링크)이 활성DS stack으로 둘 다 해소. 검증에 brief 딥링크 포함.

## Blockers / 사람 액션 대기
- 없음. 사용자가 스펙 검토 후 writing-plans 진행 지시하면 됨(스펙 승인 게이트는 새 세션 첫 단계).

## 인프라 상태
- dev 런타임 살아있음: web :62911 / daemon :62910, node@24, namespace default. `pnpm tools-dev status`로 확인, 불요시 stop.
- browse 헤드리스 세션 살아있음(brief.md 시각확인에 사용). 불요시 `~/.claude/skills/gstack/browse/dist/browse stop`.
- dev 테스트 데이터: Braze IAM 프로젝트(def55fb3)에 메시지 fc499f31 + brief.md("갱신본/2차 저장" 테스트잔여, C3서 정리 예정).

## Uncommitted
```
 M .gitignore        # gstack 툴링 — 무관, 커밋 제외
?? docs/handoffs/2026-06-24-0026-braze-brief-design.md  # 아카이브
?? docs/handoffs/2026-06-24-0836-braze-chip.md          # 아카이브
?? docs/handoffs/2026-06-24-0930.md                     # 아카이브
?? docs/handoffs/2026-06-24-1117.md                     # 직전 brief.md 트랙 아카이브
# 주의: docs/superpowers/specs/2026-06-24-bodoc-design-system-design.md = gitignored, 미표시. 디스크 존재. 새 세션서 직접 Read.
# 코드 변경 0 (설계만). brief.md 8커밋은 이미 브랜치 적재+push됨.
```

## Next Action
1. `/resume-work` → Blocking Constraints 2건 이해 표명.
2. 스펙 Read: `docs/superpowers/specs/2026-06-24-bodoc-design-system-design.md`.
3. 사용자에게 스펙 검토 확인 → 승인 시 **writing-plans 스킬**로 구현 플랜 작성.
4. 플랜대로 C1→C2→C3 구현(C1·C2는 disjoint 파일, 병렬 가능 / C3는 둘 다 필요).
