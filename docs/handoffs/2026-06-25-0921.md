---
status: in_progress
position: P0 리브랜딩 플랜 v5 완성 — 실행 대기 (리뷰 2라운드 종료)
last_updated: 2026-06-24T23:14:00Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 각 항목 이해 표명 후 진행. (v5 플랜에 이미 반영됐으나 실행 시 재확인 필수.)
- [ ] CONSTRAINT: node@24 필수 — 시스템 node 충돌. 모든 pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`.
- [ ] CONSTRAINT: **grep BRE vs ERE + 룩어헤드 금지** — 다중패턴 sweep 전부 `grep -rnE`(BRE서 `|`·`\|` 리터럴). **grep에 PCRE 룩어헤드 `(?!…)` 절대 금지**(BSD grep crash/무음 green) — 보존 예외는 `| grep -vE "$PRESERVE"` 후필터로. 룩어헤드는 perl 치환만. (v3·v4 게이트 거짓통과 근본원인.)
- [ ] CONSTRAINT: perl 치환 한줄 = **shell-safe + regex-correct 둘 다** 코드대조 검증. `.od`는 `\.od`(리터럴dot, pod/god 보호). 복잡앵커는 단일쿼트 heredoc 스크립트파일로(백틱 미노출). 탁상검토 금지.

## 현재 상태
**P0 리브랜딩(OD/Open Design → Marketing AX) 플랜이 v5로 수렴 완료. 실행 전 대기.**
리뷰 히스토리: v2(이중리뷰)→v3(3자 적대리뷰)→v4(픽스반영)→v4 2자 재검증(서브에이전트+codex 실코드대조)→**v5**. 각 라운드가 새 버그계층 포착. v5는 발견 전수반영 + **신규 헬퍼 shell 검증까지 완료**(perl 앵커·ODDIR_RE·OD_ 보존·후필터·OPEN_DESIGN_ 전부 실코드대조 PASS).

활성 플랜: `docs/superpowers/plans/2026-06-24-p0-rebranding-v5.md` (gitignored, 디스크영속, 직접 Read). v3·v4는 이력참조.

## 완료 (이번 세션)
- **v4 작성** — v3 3자 리뷰 픽스목록(Critical 5·High 4·Medium 6) 반영.
- **v4 2자 재검증** — plan-reviewer opus 서브에이전트(실코드대조) + codex(로그salvage, 세션 stall). 둘 다 v4 잔존버그 포착: v4가 Task7 grep만 고치고 동일 grep-정확성 버그를 broad-surface 태스크(10~14)에 미전파.
- **v5 작성** — Critical 4 + High 5 + Medium 3 전수반영. 단일 공유헬퍼(`PRESERVE`·`ODDIR_RE`·`oddir_*`·`write_oddir_perl`)로 grep-정확성 일괄.
- **v5 shell 검증** — 신규 헬퍼/perl 전부 실코드 PASS(constraint #3 충족).

## 잔여 — 실행 (v5 Task1~14)
- 코드 카테고리 Task1~9 **순차**(파일 공유). Task10~12c·14 = Task9 후 **병렬 가능**(disjoint). Task13 = 합류 후 최종.
- **별도 세션 권장** — 1000+파일 순차치환, 현 세션 컨텍스트 소비됨. `executing-plans` 또는 `subagent-driven-development` 스킬로.
- 실행 중 사람판단 2건(v5에 ⚠️ 표기): Task7 S5 데이터루트 escape 판정(registry.ts:53 등 RUNTIME_DATA_DIR 교체)·S4 `.od-data` alias 결정.

## 결정 (이번 세션)
- 리브랜딩 "왜" = 포크 리브랜딩(OD 업스트림 OSS → Marketing AX 별도 제품, spec:60 P0 토대, 경로1 확정). OD→MAX는 제품명 follow-on.
- AGENTS 채널정책 = Option(a) 타깃수동(Task14): AGENTS.md:145-146 + tools/pack/AGENTS.md 채널정체성 줄만, namespace·prose·@open-design패키지명·Apache귀속 보존. 블랭킷 md sweep 금지.
- v5 사용자 결정 4건: OD_LANDING_NOINDEX=보존(Q6)·루트 charts=스코프포함(Task12b helm×2)·OPEN_DESIGN_→MARKETING_AX_ + telemetry-worker placeholder(Task12c)·v5 전체반영.
- 메타: 픽스 한곳만 적용 시 동일 버그클래스 형제전파 → 공유헬퍼 단일화가 근본해소. 멀티리뷰+코드대조 가치 재입증(2라운드 다 추가버그 포착).

## Blockers / 사람 액션 대기
- **실행 = 다음 세션 첫 작업** (사용자가 실행 시점·세션 결정).
- 실행 중 Task7 데이터루트 escape/alias 판정 불명확 시 메인테이너(사용자) 확인.
- codex 재검증 세션 stall(1h23m) — 결과는 로그salvage로 회수완료, 신규발견 0(전부 v5 반영). 재실행 불요.

## 인프라 상태
- dev 런타임: 직전 세션 web :62911 / daemon :62910 살아있었으나 **날짜 변경(하루 경과) — 죽었을 가능성. 재개 시 `pnpm tools-dev status --json` 확인**.
- 리뷰 서브에이전트 2개 종료(agentId ae6f855ce2f185a32 plan-reviewer·a1a11fb7026a2e959 codex-rescue) — 재개 불요.

## Uncommitted
```
 M .gitignore                          # gstack 툴링 — 무관, 커밋 제외
 M DECISIONS.md                        # v5 결정 append됨
?? docs/handoffs/2026-06-24-1547.md    # 직전 핸드오프 아카이브
# 주의: docs/superpowers/plans/2026-06-24-p0-rebranding-v{3,4,5}.md = gitignored, 미표시. 디스크존재. v5 직접 Read.
# main: origin 미푸시 유지(27 ahead).
```

## Next Action
1. `/resume-work` → Blocking Constraints 3건(node@24·grep ERE+룩어헤드금지·perl shell+regex) 이해 표명.
2. `pnpm tools-dev status --json`로 dev 런타임 생사 확인(죽었으면 필요 시 재기동).
3. v5 Read → 실행 결정 시 **별도 세션 권장**. `executing-plans` 스킬로 Task1 Step0(헬퍼 자가검증)부터 순차.
4. 실행 시: Task1~9 순차(파일공유), Task10~12c·14 병렬, Task13 최종게이트. 각 카테고리 종료게이트(grep 0 후필터 + typecheck + 테스트핀).
5. 실행 자체는 1000+파일이라 컨텍스트 큼 — subagent-driven 고려.
