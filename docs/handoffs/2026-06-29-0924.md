---
status: in_progress
position: P2 Naver-blog 산출물 — 스펙 2라운드 리뷰 완료, writing-plans 대기
last_updated: 2026-06-28T23:46:57Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 이번 세션은 리뷰/스펙편집만(빌드 0, 실패 0). 아래는 **이전 세션 실패발견** + 이번 리뷰가 스펙에 baked한 것. **다음 액션 = 구현 진입**이라 곧 발동. 재개 에이전트는 각 항목 이해 표명 후 진행.
- [ ] CONSTRAINT: node@24 필수 — pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`. 시스템 node25(ABI141)≠node@24(ABI137) → better-sqlite3 실패. (이전 세션)
- [ ] CONSTRAINT: gh push 전 `gh auth switch --user Gmin82`. credential helper evan2942 캐시 → 403. origin=Gmin82/open-design. (이전 세션)
- [ ] CONSTRAINT: `pnpm typecheck` 전 `pnpm --filter @marketing-ax/contracts build` **먼저**. dist gitignored, stale면 TS2305/2339. 이번 작업이 contracts(projects.ts BADGE_TONES, manifest.ts) 수정 → 특히 중요. (스펙 §14에 순서 baked)
- [ ] CONSTRAINT: green 톤 시퀀싱 — ①contracts green+build → ②TONE_CLASS/CSS → ③manifest. 부팅 등록이 plugin-runtime→contracts PluginManifestSchema로 파싱 → green 미선행 시 `tone:'green'` 매니페스트 파싱/설치 **조용히 실패**(bundled.ts:166-168 warn+return, upsert 안 됨). (R2 리뷰 검증, 스펙 §11 baked)
- [ ] CONSTRAINT: i18n 키 = **19 로케일**(it.ts 포함) + types.ts Dict. AGENTS.md/구스펙이 18로 stale. 누락 시 web typecheck RED. 이번 스펙이 `homeHero.chip.naverBlog`+`.naverBlogHint` 2키 추가. (R1 리뷰 CRITICAL)
- [ ] CONSTRAINT: 배지 quiet-fail — create가 `pluginId='example-naver-blog'` 명시 전달해야 `resolveStampBadge`(project-routes.ts:761) 스탬프. prototype 기본=example-web-prototype 매핑이라 누락 시 배지 조용히 사라짐. 칩 action + CLI create 둘 다 검증. (이전+R1 CONFIRMED)

## Anti-Patterns (이번 세션 발견)
| 패턴 | 내용 | 심각도 | 회피책 |
|---|---|---|---|
| 패치 본문만 수정 | 스펙 본문 §7/§10/§11 고치고 미러 위치(§2 요약표·§13 파일목록·§16 리스크표) 미전파 → 모순 4건 | advisory | 사실 수정 시 그 사실을 재진술하는 모든 섹션 동시 갱신 |
| 자가편집 확증편향 | 내가 추가한 drift-guard `diff==0`가 manifest 비대칭(정본만 open-design.json)으로 영구 RED인데 자가검토서 못 잡음. 독립 plan-reviewer가 포착 | advisory | 스펙 자가편집 후 독립 서브에이전트 검증 필수 |
| 전체디렉터리 diff 가드 | 정본/미러 파일셋 비대칭이면 `diff(dir,dir)==0` 불가 | blocking(가드 작성 시) | 공유 서브셋만 byte-identical 비교, 정본-only 파일 제외 |

## 현재 상태
P2 2번째 버티컬 = **네이버 블로그 산출물** 스펙. 2라운드 교차리뷰(codex consult + plan-reviewer ×2) 완료 → 발견 결함 전부 정정 → 스펙 내부정합+codebase검증 통과. **다음 = writing-plans 스킬로 구현 플랜 작성** (브레인스토밍 종착점, 다른 구현 스킬 금지).

스펙 파일(gitignored, 디스크 영속): `docs/superpowers/specs/2026-06-26-naver-blog-deliverable-design.md`
설계 = Braze IAM 경량(Path A) 복제. 데몬 코드 0. 3층(스킬/craft/bodoc DESIGN.md).

## 완료 (이번 세션)
- `/resume-work` 복원(이전 HANDOFF → `docs/handoffs/2026-06-26-1638.md` 아카이브).
- **R1 리뷰**: codex consult(스펙 검증, 6 findings) + plan-reviewer agent(APPROVE-WITH-CHANGES, 6 errors) 병렬. 교차분석.
- 사용자 결정 2건(AskUserQuestion): 2-카탈로그=미러유지+가드 / 스펙수정=전부적용.
- **스펙 패치 R1**: 사실오류 6건 정정(로케일 18→19, switch 3→2, manifest i18n 17→18, pipeline이유, countNewHtmlArtifacts=analytics, z.enum폴백삭제) + green선행 시퀀싱 + CLI 2-스텝 + 드리프트가드 추가.
- 사용자 재리뷰 요청 → **자가 grep + 전파모순 4건 수정**(§2 D5 / §13 HomeView / §16 폴백·드리프트중복).
- **R2 재리뷰**: plan-reviewer agent → drift-guard `diff==0` 결함 발견(manifest 비대칭). 자가 가설(frontmatter name)은 틀렸으나 같은영역 가리켜 agent가 진짜원인 포착. → 수정 5건(가드=공유서브셋 byte-identical, open-design.json 제외; HomeView 편집불필요 확정; static-resource 라인정정).
- 최종 grep 클린(stale diff==0/추정 0).

## 잔여
1. **writing-plans 스킬** 호출 → 구현 플랜 작성 (현재 게이트)
2. 구현 — 신규 ~6(번들 정본+미러 카탈로그, craft/naver-blog-html.md, badge red-spec, catalog-sync 가드테스트) / 수정 ~9 (스펙 §13)
3. 검증 (스펙 §14 — 명령순서 contracts build→typecheck, CLI dual-track 수락, 드리프트가드)
4. PR

## 결정 (이번 세션 — DECISIONS.md append됨)
- 2-카탈로그 = Braze 미러 유지 + **공유 서브셋 byte-identical 가드**(SKILL.md/example.html/references/**, open-design.json 제외). 전체 `diff==0`은 정본만 manifest 보유라 영구 RED → 불가.
- 스펙 결함 11건 전부 정정 = R1 사실오류 6 + R2 전파모순 4 + R2 drift-guard결함 1.
- 메타: 자가편집 확증편향 → 독립 plan-reviewer 검증이 본문-미러 모순 + 신규 주입결함 둘 다 포착. 2라운드 리뷰 가치 입증.
- codex 결과 자동반영 안 함(글로벌 룰) — 사용자 결정 후 적용.

## Blockers / 사람 액션 대기
- 없음. 게이트 = 사용자가 writing-plans 진입 승인.

## 인프라 상태
- dev 런타임 미기동. 이번 세션 빌드 0회.
- 워크플로 트랜스크립트 잔존(무해): `wf_69f3df9d-979`(braze 매핑, 이전).
- contracts dist 빌드상태 미확인 — 구현 진입 시 `pnpm --filter @marketing-ax/contracts build` 먼저(CONSTRAINT).

## Uncommitted
```
 M DECISIONS.md
 M docs/marketing-ax-roadmap.md
?? docs/handoffs/2026-06-26-1553.md
?? docs/handoffs/2026-06-26-1638.md
```
(스펙은 docs/superpowers/ gitignored — git status 미표시, 디스크 영속. 이번 세션 스펙편집 다수지만 tracked 변경 아님)

## Next Action
새 세션: `/resume-work` → 스펙 `docs/superpowers/specs/2026-06-26-naver-blog-deliverable-design.md` 읽기(2라운드 리뷰 반영 최종본). 사용자 writing-plans 진입 승인 시 → `writing-plans` 스킬로 구현 플랜 작성. (구현 순서 = CONSTRAINT의 green 시퀀싱 ①contracts ②web ③manifest 준수)
