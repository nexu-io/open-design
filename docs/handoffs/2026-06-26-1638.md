---
status: in_progress
position: P2 Naver-blog 산출물 — 스펙 작성+self-review 완료, 사용자 리뷰 대기
last_updated: 2026-06-26T07:31:02Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 이번 세션(브레인스토밍/스펙)은 빌드 실패 0. 아래는 **구현 페이즈 진입 시** 적용될 검증된 제약(이전 세션 발견 + 이번 스펙이 직접 거는 것). 재개 에이전트는 각 항목 이해 표명 후 진행.
- [ ] CONSTRAINT: node@24 필수 — pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`. 시스템 node25(ABI141)≠node@24(ABI137) → better-sqlite3 실패. (이전 세션)
- [ ] CONSTRAINT: gh push 전 `gh auth switch --user Gmin82`. credential helper evan2942 캐시 → 403. origin=Gmin82/open-design. (이전 세션)
- [ ] CONSTRAINT: `pnpm typecheck` 전 `pnpm --filter @marketing-ax/contracts build`. dist gitignored, stale면 TS2305/2339. (이전 세션) — 이번 작업이 contracts(projects.ts BADGE_TONES, manifest.ts) 수정하므로 특히 중요.
- [ ] CONSTRAINT: i18n 키 추가 = 18 로케일 전부 + types.ts Dict. 누락 시 web typecheck RED. 이번 스펙이 `homeHero.chip.naverBlog`+`.naverBlogHint` 2키 추가 요구.
- [ ] CONSTRAINT: 배지 quiet-fail — create가 `pluginId='example-naver-blog'` 명시 전달해야 `resolveStampBadge`(project-routes.ts:761)가 스탬프. 기본 prototype→example-web-prototype 매핑이라 누락 시 배지 조용히 사라짐. 칩 action + CLI create 둘 다 검증.

## 현재 상태
P2 2번째 버티컬 = **네이버 블로그 산출물** 착수. brainstorming(superpowers) 완주 → 설계 확정 → 스펙 문서 작성 + self-review 완료. **다음 = 사용자가 스펙 리뷰**.

스펙 파일(gitignored 경로, 디스크 영속): `docs/superpowers/specs/2026-06-26-naver-blog-deliverable-design.md`

설계 = Braze IAM 패턴 **경량(Path A) 복제**. 데몬 코드 0(신규 라우트/SQLite/contracts DTO/CLI 서브커맨드 없음). 3층 분리 = 스킬(범용 채널 워크플로) / craft(네이버 SmartEditor 13 HTML룰+SEO) / bodoc DESIGN.md(보험 특화 사실).

## 완료 (이번 세션)
- **컨텍스트 수집**: HANDOFF 복원(P1 출하 확인) → 로드맵 읽기 → Braze IAM 패턴 8-서브시스템 매핑 워크플로(wf_69f3df9d-979, 9에이전트) → bodoc `blog-cardnews` 참고 폴더 도메인 digest(Explore) → Path A 파일쓰기 메커니즘 검증(Explore).
- **brainstorming**: 게이팅 질문 4건 + 배지 1건 사용자 결정 → 3층 설계 확정.
- **스펙 작성**: 16섹션 + 부록. self-review 통과(craft↔references 중복 모호성 1건 인라인 수정).
- HANDOFF(이전) 아카이브: `docs/handoffs/2026-06-26-1553.md`.

## 잔여
1. **사용자 스펙 리뷰** (현재 게이트) — 수정점 있으면 반영+재검토 / OK면 ↓
2. spec commit?? — 단 spec은 gitignored. roadmap이 tracked 진실원. (commit 대상 = 아카이브된 handoff뿐)
3. **writing-plans 스킬** 호출 → 구현 플랜 작성
4. 구현 (신규 ~5 / 수정 ~9 파일 — 스펙 §13)
5. 검증 (스펙 §14) → PR

## 결정 (이번 세션 — DECISIONS.md append됨)
- D1: 이미지 생성 X, 글(HTML)만 — 사용자 지시. 썸네일/배너 PNG 비범위.
- D2: Path A 원샷 제네릭 — 블로그=단일 카피헤비 HTML. 데몬 코드 0.
- D3: 브랜드-범용 스킬 + bodoc DESIGN.md 보험특화 — 스킬은 채널규약만, 보험사실은 활성 DESIGN.md.
- D4: SEO 룰만 베이크 — 라이브 SERP 스크래핑(check-naver-rank) 비범위.
- D5: 출력영속 = 에이전트 cwd 직접 Write(brief.md+HTML) — dual-track-safe, 라우트 0.
- D6: 배지 Naver그린 신규 + 톤 리스트 단일 SoT 리팩터(향후 산출물 배지 확장성).
- P2 추상화: 아직 안 함 — 2 데이터포인트(Braze 무거운레인 / Naver 경량레인)로 보아 3번째 추적형 라이프사이클 등장 시 추출.

## Blockers / 사람 액션 대기
- 없음. 게이트 = 사용자 스펙 리뷰 응답.

## 인프라 상태
- dev 런타임 미기동. 워크플로 트랜스크립트 잔존(무해): `wf_69f3df9d-979`(braze 매핑).
- contracts dist 빌드 상태 미확인 — 구현 진입 시 `pnpm --filter @marketing-ax/contracts build` 먼저.

## Uncommitted
```
?? docs/handoffs/2026-06-26-1553.md
```
(스펙은 docs/superpowers/ gitignored — git status 미표시, 디스크 영속)

## Next Action
새 세션: `/resume-work` → 스펙 파일 `docs/superpowers/specs/2026-06-26-naver-blog-deliverable-design.md` 읽고 사용자에게 리뷰 받기. 승인 시 `writing-plans` 스킬로 구현 플랜 작성 진입. (브레인스토밍 종착점 = writing-plans, 다른 구현 스킬 금지)
