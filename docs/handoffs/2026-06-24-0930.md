---
status: in_progress
position: Braze IAM brief.md 저장 — 설계+플랜 완료(검증 통과), 구현 미착수 0/8
last_updated: 2026-06-24T00:26:23Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 재개 에이전트는 각 항목 이해를 표명한 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 시스템 node v25라 pnpm/typecheck/better-sqlite3 깨짐. 모든 pnpm/node 명령 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`.
- [ ] CONSTRAINT: 데이터-경로 = RUNTIME_DATA_DIR 파생만 — brief.md 쓰기는 반드시 `writeProjectFile(PROJECTS_DIR, projectId, relPath, body, opts, project.metadata)` 경유. imported-folder 프로젝트는 `project.metadata`(baseDir) 전달 필수. cwd-상대/임의경로 하드코딩 금지(AGENTS.md Daemon data directory contract).
- [ ] CONSTRAINT: dual-track 필수 — brief 저장/조회는 web(파일 탭 + 상세 링크) + `od braze brief`/`od braze get` 양쪽, 같은 `/api/braze/*` 호출. 한쪽만 = regression.
- [ ] CONSTRAINT: bodoc = PII/예시브랜드 — SKILL brief 템플릿/daemon 로직 브랜드-애그노스틱 유지. bodoc 식별자(`bodoc://`·보닥명·attributes) 하드코딩 금지. 브랜드 사실은 활성 design_system에서만.

## 현재 상태
**설계+구현플랜 = 완료·이중검증 통과. 코드 구현은 0/8 미착수.**
- 산출물 2종(디스크 존재, **`docs/superpowers/`는 gitignored → git 미추적**, 새 세션서 직접 Read):
  - 스펙: `docs/superpowers/specs/2026-06-24-braze-brief-md-design.md` (v2, codex 13건 반영)
  - 플랜: `docs/superpowers/plans/2026-06-24-braze-brief-md.md` (v2, plan-reviewer 9건 반영, 8 tasks TDD)
- 요구: Braze IAM 기획안 컨펌(plan_confirmed) 시 SKILL이 디벨롭된 brief.md 마크다운 저작 → daemon이 디자인 프로젝트 파일로 저장 → web 파일 탭 + `od braze brief` CLI 노출.

## 완료 (이번 세션)
- **brainstorming → 설계 미결정 4건 합의**: 생성주체=접근 A(SKILL/에이전트 저작, daemon은 경로·저장·표면노출만), 저장위치=`PROJECTS_DIR/<projectId>/braze/<messageId>-<slug>/brief.md`, 데이터계약=plan 스키마 불변 + `briefPath` 1필드, 트리거=신규 `POST /api/braze/messages/:id/brief`(confirm 분기 불변).
- **codex review(consult, 0.135.0)** — 13 findings. C1~C10·C13 스펙 직접반영, C7·C8·C12 스코프 현실화, C11(confirm 비멱등) 별도 follow-up. → 스펙 v2.
- **plan-reviewer review(opus 임베드 — 심링크 레지스트리 미반영으로 general-purpose+opus 대체)** — 9 findings 전부 file:line 검증. → 플랜 v2:
  - C-1 getProject=`./db.js`(projects.ts 아님), C-2 `paths: pathDeps`(부분객체 typecheck 실패), C-3 `navigate({kind:'project'})`(openProjectFile 없음)+MessageDetail 배선, C-4 라우트/CLI 테스트 하네스 **신규구축**(기존 braze 테스트=순수 단위테스트뿐), S-1 `--brief-file`, S-2 `readPromptFromFlags`(cli.ts:7308) 재사용, S-3 manifest 추론 명기, N-1 로케일 19(it.ts), 갭 3건(충돌/baseDir/route briefPath) Task화.

## 잔여 (구현 0/8 — 플랜대로)
- Task 1: SKILL plan JSON 계약 드리프트 교정(선결 — iam_format→iamFormat 등, SKILL.md:203)
- Task 2: contracts — briefPath + BrazeBriefSaveRequest/Response
- Task 3: persistence — brief_path 컬럼 PRAGMA+ALTER 마이그레이션 + 매핑/패치
- Task 4: daemon route — POST .../brief (deps `paths: pathDeps`, getProject from db.js, status allowlist, writeProjectFile, 에러매핑) + 라우트 테스트 하네스
- Task 5: CLI — `od braze brief --brief-file <path|->` + `od braze get` briefPath + CLI 테스트 하네스
- Task 6: web — BrazeSection navigate 링크 + i18n 19로케일
- Task 7: SKILL — 컨펌 후 brief.md 저작 단계 + 완료텍스트
- Task 8: 통합 검증(guard/typecheck/daemon test/web typecheck)

## 결정
- 생성주체 = 접근 A(SKILL 저작 + daemon 저장만) — 참조 brief가 LLM 산문 다수(선정근거·디자인방향·가설)라 결정론 템플릿(B) 재현 불가, OD 기존 패턴 정합 — 근거 스펙 §2
- 검증 = codex(스펙) + plan-reviewer(플랜) 이중 적대검증 — 멀티서피스 기능은 격리리뷰 부족, 통합검증 필수(직전 PR #1 교훈 연장)
- plan-reviewer 심링크(`.claude/agents/plan-reviewer.md` → service-dev)는 세션시작 후 추가 → 레지스트리 미반영. 직접호출 불가, 페르소나 임베드로 우회. **새 세션선 직접 호출 가능**

## Blockers / 사람 액션 대기
- 없음. (PR #1 스크린샷 사람 캡처 대기는 별건·기존)

## 인프라 상태
- dev 런타임: 이전 세션서 띄웠을 수 있음(web :17573 / daemon :17456, node@24). 확인 `pnpm tools-dev status`, 불요시 stop.
- 테스트 dev 데이터: 프로젝트 `braze-demo`·`Braze IAM`(이전 세션 잔여). 정리 안 함.

## Uncommitted
```
 M .gitignore        # gstack 툴링 +.gstack/ — 무관, 커밋 제외
 M DECISIONS.md      # 이번 세션 결정 append
?? HANDOFF.md        # 이 파일
?? docs/handoffs/2026-06-24-0026-braze-brief-design.md  # 직전 HANDOFF 아카이브
?? docs/handoffs/2026-06-24-0836-braze-chip.md          # 더 이전 아카이브
# 주의: docs/superpowers/{specs,plans}/2026-06-24-braze-brief-md*.md = gitignored, git status 미표시. 디스크 존재.
```

## Next Action
1. `/resume-work` → 위 Blocking Constraints 4건 이해 표명.
2. 플랜 Read: `docs/superpowers/plans/2026-06-24-braze-brief-md.md` (gitignored, 디스크서 직접).
3. **구현 시작**: `superpowers:subagent-driven-development`로 Task 1부터 순차(task별 fresh subagent + 리뷰 게이트). node@24 export 먼저.
4. Task 간 의존: 2→3→4 순서 필수(contracts→persistence→route). 1·7(SKILL)·6(web)은 비교적 독립. Task 4·5는 테스트 하네스 신규구축 부담(플랜 명시).
