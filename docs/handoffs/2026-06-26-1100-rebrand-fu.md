---
status: in_progress
position: 리브랜드 후속 — #1(stale 문서 참조) 완료·푸시. ②~⑤ 미착수.
last_updated: 2026-06-26T00:36:12Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
> 실패로 발견된 제약만. 재개 에이전트는 각 항목 이해를 표명한 후 진행.
- [ ] CONSTRAINT: node@24 필수 — 모든 pnpm/node 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`. 시스템 node25(ABI141)≠node@24(ABI137) → better-sqlite3 네이티브 import 실패 → daemon 테스트 대량 false-fail.
- [ ] CONSTRAINT: shell=zsh — `$PIPESTATUS` 빈값(zsh는 `$pipestatus`). 파이프 exit 필요시 `cmd >log 2>&1; echo $?`.
- [ ] CONSTRAINT: grep=`grep -rnE`(ERE). PCRE 룩어헤드 `(?!…)` BSD grep crash/무음 금지. 보존은 `| grep -vE` 후필터.
- [ ] CONSTRAINT: perl 브랜드 스왑 구분자 — `s!…!…!`는 `(?!` 룩어헤드와 **충돌**(이번 세션 실측). `s#…#…#g` 사용. `s{}{}`도 brace-class 파싱실패. 기본 `#` 구분자.
- [ ] CONSTRAINT: gh 계정 — origin=`Gmin82/open-design`. push 전 `gh auth switch --user Gmin82` 필수(evan2942=403). credential helper는 active gh 계정 사용.
- [ ] CONSTRAINT: 서브에이전트 git 조작 금지 — checkout/reset/rebase/commit/branch/worktree 전부. 메인스레드가 git 전담, 에이전트는 Edit/Write+검증만. (P0 Task5 orphan 사고 교훈, 이번 세션 7 에이전트 전부 준수.)

## 현재 상태
P0 리브랜드 후속 작업 진행 중. 이번 세션 = **후속 3건(landing/data-dir/mcp-bind) + #1(stale 문서 참조) 완료·origin 푸시**. main==origin/main(`92fac292e`), 트리 clean. 모든 작업 SDD worktree 격리 병렬, 메인스레드 git 전담.

## 완료 (이번 세션, 전부 push됨)
- **후속 C(mcp conversation-binding)**: 현재 main서 이미 fixed(상류 #3242 `9a3424d68`가 `convs[0]`→createdAt정렬). source 0, regression guard `apps/daemon/tests/mcp-run-conversation-bind.test.ts`(revert→RED 증명).
- **후속 B(RUNTIME_DATA_DIR escape)**: daemon 호출처 4곳 전부 이미 `PLUGIN_REGISTRY_ROOTS` 전달 → escape 도달불가. source 0, guard `apps/daemon/tests/plugins-registry-roots-escape.test.ts`(mutation→RED).
- **후속 A(landing-page 리브랜드)**: 협소 슬라이스 — 표시텍스트 `Open Design`→`Marketing AX`만(118파일). 도메인/슬러그/blog본문/SEO별칭 보존. astro build 3304페이지 PASS.
- **#1 stale 문서 참조**: 3-병렬(G1 루트13·G2 docs/74·G3 AGENTS13 = 100파일). 깨진 `pnpm --filter @open-design/*` 300+ 명령 → `@marketing-ax/*` 복구. 실작동 검증(web pkg resolves OK). 활성 가이드 broken-ref 0.
- 게이트: guard 63/63 · typecheck PASS · 신규 daemon spec 3 green.

## 잔여 (우선순위 순 — 별도 결정/진행)
1. **②README/docs 더 깊은 리브랜드**: 대부분 #1 G1/G2서 완료. 남은 건 bare `OD` 약어("Marketing AX (OD)", "Once OD is installed") 처리 여부 = 결정 필요(OD→MAX? 유지?).
2. **③landing 패키지명** `@open-design/landing-page`→`@marketing-ax/landing-page` (+AGENTS.md filter 4줄·CF workflow 참조·pnpm-workspace). 코드체인 아님(disjoint).
3. **④landing `OD_LANDING_*` env**(13) → `MAX_LANDING_*` 리네임 여부.
4. **⑤non-md brand**: `docs/assets/_cover/*.html`(3)·`docs/schemas/open-design.*.json`(스키마 $id/title) — #1 doc 스코프(*.md)서 제외됨.
5. **blog/tutorial 본문**(1987 mention)·**SEO alias 배열**(`OpenDesign`/`Open Design AI` JSON-LD alternateName) — 편집/SEO 결정 필요.
6. **아카이브 defer**: `docs/superpowers/plans|specs/`(4파일 broken-ref 26)·`specs/` — 과거기록이라 스왑=왜곡, 보류.
7. **코드품질(브랜드무관)**: B 타입약화 `routes/plugins` `string[]`→`RegistryRoots` 컴파일강제화.

## 결정 (이번 세션 — DECISIONS.md 이미 append 완료)
- 후속 3건 = SDD disjoint worktree 병렬, B/C 먼저 후 A. B/C 둘 다 이미-fixed 판명 → blind fix 금지, regression guard만+teeth 증명.
- A landing = 도메인보존·슬러그보존·blog본문제외(편집결정)·표시텍스트만(사용자 AskUserQuestion 3건).
- #1 스왑맵 = on-disk `@marketing-ax/X` 존재 22 suffix만. landing-page/tools-pr/cli = 자동보존. upstream귀속/트레이드마크 prose + `nexu-io/open-design` slug 보존.

## Blockers / 사람 액션 대기
- 없음. (push 성공, gh active=Gmin82.)
- 잔여 ②(OD약어)·④(env)·⑤(non-md)·blog본문·SEO별칭은 **사용자 결정 필요** 항목.

## 인프라 상태
- 백그라운드 프로세스 없음. worktree 전부 제거(`/private/tmp/fu-wt/*` 없음). agent worktree 2개(`.claude/worktrees/agent-*`) 무관·미접촉.
- 환경 위생: better-sqlite3=node@24 ABI137 재빌드 유지. main repo `.max/` 런타임데이터(gitignored) 잔존 무해.

## Uncommitted
```
(none — 트리 clean, main==origin/main=92fac292e)
```

## Next Action
재개 시:
1. `/resume-work` → Blocking Constraints 6건 표명 + `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"` + `gh auth status`(active=Gmin82) 확인.
2. 잔여 우선순위 사용자 결정: ②OD약어 처리 / ③landing 패키지명 / ④landing env / ⑤non-md brand / blog본문·SEO별칭. ③+④는 SDD 병렬 가능(landing 패키지명+env disjoint), ⑤는 빠른 단발.
3. DECISIONS.md tail 30줄 Read로 이번 세션 결정 맥락 복원.
