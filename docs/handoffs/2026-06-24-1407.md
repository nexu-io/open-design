---
status: in_progress
position: bodoc 디자인시스템 포팅 — 구현 2/3 완료(머지레디), Task 3(e2e produce+시각게이트+정리) user-gated 잔여
last_updated: 2026-06-24T02:00:00Z
---

# Blocking Constraints — 재개 시 먼저 읽고 체크
- [ ] CONSTRAINT: node@24 필수 — 시스템 node v25라 pnpm/typecheck/better-sqlite3 깨짐. 모든 pnpm/node 명령 앞 `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`.
- [ ] CONSTRAINT: od CLI·daemon 둘 다 `apps/daemon/dist/cli.js`서 구동 — daemon src 변경 후 `pnpm --filter @open-design/daemon build` + 재기동 필수. (이미 Task2 후 rebuild+restart 완료, 현재 런타임 최신.)

## 현재 상태
**bodoc 디자인시스템 포팅 트랙. SDD+TDD로 구현. Task 1·2 완료·리뷰·final-review YES(머지레디). Task 3만 잔여(user-gated).**
- 플랜: `docs/superpowers/plans/2026-06-24-bodoc-design-system-port.md` (committed? gitignored docs/superpowers → 디스크 영속, 직접 Read).
- 스펙: `docs/superpowers/specs/2026-06-24-bodoc-design-system-design.md`.
- SDD ledger: `.superpowers/sdd/progress.md` ("bodoc design system port" 섹션) — Task별 커밋·리뷰 결과.

## 완료 (이번 세션)
- **Task 1** (commit a7294807b + 51d448b4c): `design-systems/bodoc/DESIGN.md` 생성 — prose-only(DESIGN.md 단일). 토큰 :root·Braze 폰트자산문자열4·딥링크13·voice·anti-patterns 임베드. 컨트랙트 테스트(`apps/daemon/tests/design-system-bodoc-contract.test.ts`). **스펙 이탈**: tokens.css 단독 포팅이 guard(페어링+토큰스키마) FAIL이라 4파일 대신 DESIGN.md 안에 토큰 임베드(전체 body 주입되므로 정보 동일). + guard 수정(`scripts/check-components-manifest-extraction.ts` prose-only ENOENT → 3-branch XOR 명시). 리뷰 Approved.
- **Task 2** (commit 1dd2ed080): 칩 `plugins/_official/examples/braze-iam/open-design.json` `designSystem {primary:true}`→`{ref:"bodoc"}`. 바인딩 테스트(`braze-chip-design-system-bind.test.ts`, `pickDesignSystemId`==="bodoc"). `apply.ts` pickDesignSystemId export 추가(test-only). 리뷰 Approved 0 issues.
- **Final whole-branch review**(7a2f75566..1dd2ed080, 3커밋): **Ready to merge YES, 0 blocking**. XOR-fail 경로 fixture로 재현, 5개 DS guard prose-only skip 확인, 콘텐츠 정합(딥링크13·#16C5FF), 칩 정확, apply.ts export-only.
- **라이브 검증**: rebuild+restart 후 `GET /api/design-systems`에 bodoc 노출(id "bodoc", title "보닥(bodoc) IAM", Marketing). 칩→bodoc 해소는 유닛테스트 증명. produce/brief에 활성DS DESIGN.md 전체주입은 코드경로 확인(system.ts:665, panel.ts <BRAND_SOURCE>).

## 잔여 — Task 3 (user-gated, 플랜 Step 1-8)
**왜 user-gated**: (a) 실제 IAM produce = LLM 에이전트 런(사용자 계정 비용·수분), (b) Step 5 시각확인 = 사람 게이트.
1. (완료) 런타임 최신화 — rebuild+restart 끝. daemon :62910 / web :62911 / namespace default / node@24.
2. bodoc 바인딩 프로젝트서 IAM 새로 produce (칩 또는 `od braze` produce). variant HTML 경로 캡처.
3. variant HTML grep: `#16C5FF`≥1 + `bodoc://action/`≥1 + `#4f46e5|#0f172a`=0 + `yourapp://|example.com/signup`=0.
4. brief 재저작(interview→plan→confirm→`od braze brief`) → brief.md grep: `bodoc://action/`≥1 + `yourapp://|지금은 플레이스홀더`=0.
5. **시각확인(사람)**: variant를 web FileViewer로 렌더, 보닥 시그니처(#16C5FF cyan, Pretendard, slop 없음) 눈 확인. PASS/FAIL 기록.
6. 정리: 테스트잔여 brief.md("갱신본/2차 저장") 제거 — dev 프로젝트 `braze/<messageId>-<slug>/`. 신규 Task3 산출물은 보존.
7. final 검증 게이트: `pnpm guard` + `pnpm typecheck` + 두 컨트랙트 테스트 PASS (chat-route.test.ts:298 기존결함은 무관·회귀아님).
8. cleanup/로그 커밋.

## 결정 (이번 세션 — DECISIONS.md append됨)
- **스펙 이탈(중요)**: bodoc DS = `design-systems/bodoc/DESIGN.md` 단일(prose-only). 스펙은 tokens.css/json/braze-fonts.css 4파일 포팅이었으나 코드검증 결과 tokens.css 단독 = `pnpm guard` FAIL(check-tokens-fixture-sync 페어링 + 토큰스키마 A1/A2/B-slot 요구). → 토큰·폰트자산·딥링크 전부 DESIGN.md 안에 fenced 임베드. daemon이 DESIGN.md 전체 body를 produce+brief에 주입(system.ts:665)하므로 정보 동일 도달. 스펙 의도(옵션A·manifest 생략) 보존.
- 칩 바인딩 키 = `ref` (not id/path). `pickDesignSystemId`(apply.ts:326) `ds.ref.trim()`.
- SKILL.md 변경 0 — 이미 `design-systems/<brand>/DESIGN.md`+딥링크 로드 지시, 하드코딩 금지.
- guard 수정 = check-components-manifest-extraction.ts 3-branch(both-absent skip / XOR pairingError→fail / both-present process), 형제가드 check-tokens-fixture-sync 패턴 미러.

## Blockers / 사람 액션 대기
- Task 3 produce 실행 방식: 사용자가 "browse로 직접 테스트" 지시 → Claude가 헤드리스 browse로 구동 중.
- **browse 진행상태(중단점)**: web :62911 홈 로드, Braze IAM 칩 클릭→선택됨(home-hero__type-tab, @e13 pressed). 다음 = 작업디렉토리 선택(@e16 "Select working directory") + 쿼리 입력(@e11 combobox) → Run(@e15, 현재 disabled) → 인터뷰 question-form → plan → confirm → produce. browse 데몬 살아있음.
- **중단 사유**: 컨텍스트 204K(위생경고). 풀 헤드리스 인터뷰→produce는 다중 스냅샷+수분 produce 폴링이라 세션 소진 위험. → /clear + /resume-work 후 새 세션 풀 컨텍스트로 produce 권장.
- 기존 "Braze IAM"(Completed·5h前) 프로젝트 산출물 = bodoc 변경 前이라 검증 무용. 새 produce 필수.

## 인프라 상태
- dev 런타임 살아있음(rebuild+restart 완료): web :62911 / daemon :62910 / node@24 / namespace default. tools-dev 백그라운드 로그: scratchpad/tools-dev.log.
- dev 테스트데이터: Braze IAM 프로젝트 `def55fb3-af91-4adf-8a16-545be0cccffc` (+ `braze-demo`). 메시지 fc499f31 + 테스트잔여 brief.md(Step 6서 정리 예정).

## Uncommitted
```
 M .gitignore        # gstack 툴링 — 무관, 커밋 제외
 M DECISIONS.md      # 이번 트랙 결정 append
?? HANDOFF.md        # 본 파일
?? docs/handoffs/2026-06-24-1117.md 등 아카이브
# 주의: docs/superpowers/{specs,plans}/2026-06-24-bodoc-* = gitignored, 미표시. 디스크 존재. 직접 Read.
# .superpowers/sdd/ = gitignored scratch (ledger·brief·report·diff). 디스크 존재.
# bodoc 트랙 코드 3커밋(a7294807b·51d448b4c·1dd2ed080) 이미 브랜치 적재.
```

## Next Action
1. `/resume-work` → Blocking Constraints 2건 이해 표명.
2. 사용자에게 Task 3 produce 실행 방식 확인(A: CLI 구동 / B: 직접 칩). 
3. produce → HTML/brief grep(Step 3·4) → 시각확인(Step 5, 사람) → 정리(Step 6) → 검증게이트(Step 7) → 커밋(Step 8).
4. Task 3 PASS 후: finishing-a-development-branch로 머지/PR 결정. (코드는 이미 final-review YES.)
